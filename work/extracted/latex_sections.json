{
  "sec/abstract.tex": "% !TEX root = ../main.tex\n\n Diffusion Language Models (DLMs) have recently achieved strong results in text generation. However, their multi-step sampling leads to slow inference, limiting practical use. To address this, we extend Inverse Distillation, a technique originally developed to accelerate continuous diffusion models, to the discrete setting.\n Nonetheless, this extension introduces both theoretical and practical challenges. From a theoretical perspective, the inverse distillation objective lacks uniqueness guarantees, which may lead to suboptimal solutions. From a practical standpoint, backpropagation in the discrete space is non-trivial and often unstable. To overcome these challenges, we first provide a theoretical result demonstrating that our inverse formulation admits a unique solution, thereby ensuring valid optimization. We then introduce gradient-stable relaxations to support effective training. \n As a result, experiments on multiple DLMs show that our method, , reduces the number of inference steps by $4 $-$64 $, while preserving the teacher model\u2019s entropy and generative perplexity.",
  "sec/introduction.tex": "% !TEX root = ../main.tex\n\n [ht!]\n\n {An overview of Diffusion Language Models (DLMs) (top) and the proposed (bottom). DLMs train a denoising network $ $ using samples from the target data distribution $p^*(x_0)$. In contrast, our approach leverages inverse distillation to learn\n $p_{ }(x_0)$ such that the resulting solution recovers the pretrained model $f^*.$\n Consequently, while standard DLMs exhibit strong generative performance, they typically require\n % incur high inference latency due to \n the large number of sampling steps. IDLM instead trains a few-step generator that maintains generative quality while substantially accelerating inference.}\n\n % Generative modeling for discrete data, such as natural text and code,\n % has gained widespread adoption as the output of large language models~ \n % becomes increasingly indistinguishable from real-world human data in terms of perplexity and quality~ .\n % Recently, Diffusion Language Models (DLMs; )\n % have emerged as promising framework for modeling the distribution of text data. General framework for broad class of DLMs was established by SEDD~ model. However, the general case of DLMs is generally intractable in practice and leads to overcomplicated theoretical part. To overcome this issue, further works~ concentrated on specific types of processes and as result simplified theoretical formulation of DLMs. It leads to the fact that DLM achieved comparable performance in the field of text generation in comparison with dominant paradigm autoregressive (AR) language models~ . In contrast to AR models DLMs offer the benefits of flexible, parallel generation. However, the iterative nature of the reverse diffusion process necessitates hundreds or thousands of sampling steps,\n % creating a significant latency bottleneck that hinders practical use.\n\nGenerative modeling for discrete data, such as natural language, has seen widespread adoption, driven by the rapid advancement of large language models~ . \n% The outputs of these models have become increasingly indistinguishable from human-generated content in terms of both perplexity and perceptual quality~ . \nRecently, Diffusion Language Models \n(DLMs; ) have emerged as a promising framework for modeling the distribution of the text data. A general formulation for a broad class of DLMs was introduced by~ . However, the general formulation is often intractable in practice and introduces significant theoretical complexity. To address this, subsequent work~ has focused on specific types of diffusion processes, enabling simplified theoretical analyses and more tractable implementations. As a result, DLMs have achieved competitive performance in text generation, comparable to that of autoregressive language models~ . However, the inherently iterative nature of reverse diffusion requires hundreds or even thousands of sampling steps at inference time, resulting in considerable latency that limits their practicality.\n\nOn the other hand, in the continuous domain, the issue of slow inference in diffusion models~ has been the focus of extensive research~ . Among the various approaches, has emerged as one of the most effective solutions~ . However, directly applying such techniques in the discrete domain is challenging due to the non-differentiable nature of discrete data. Recent efforts have begun to address this limitation for DLMs. Notably, SDTT~ and Duo-DCD~ propose adaptations of consistency-based distillation methods~ to the discrete setting, demonstrating initial progress toward enabling fast inference in DLMs.\n % By leveraging continuous-time Markov\n % chains to corrupt data and subsequently learning to reverse this noise process, architectures like SEDD~ \n % and MDLM~ have demonstrated perplexity scores competitive with state-of-the-art autoregressive language models while offering the benefits of flexible, parallel generation.\n\n % On the other side in the domain of continuous diffusion models~ the longstanding problem of slow inference trying to solve it for a long time~ . most notable example among all of these approaches is distillation~ . However, practically it is very hard to directly apply these technique due to the non-differentiable nature of the data. Recent works have begun\n % to address this for DLMs: SDTT~ and Duo-DCD~ \n % employs adaption distillation technique, known as consistency models. \n\n % In our work we employ another direction of distillation. Specifically, we extend the theoretical framework of ~ for continuous models to the discrete setting. This yields , a principled framework for accelerating a broad class of discrete diffusions.\n % Our main idea, is instead of training the student directly to mimic the teacher's outputs, we treat the pre-trained teacher as the fixed optimal solution to a denoising objective. We then optimize the student distribution to be the one for which this teacher is optimal. \n In this work, we investigate an alternative distillation paradigm by extending the theoretical framework of ~ , originally developed for continuous diffusion models, to the discrete domain. However, this direct extension introduces several nontrivial challenges arising from the intrinsic properties of discrete data. First, emerge regarding the validity of the training objective, particularly due to the potential non-uniqueness of its minimizers. Second, arise from the non-smoothness of the discrete space, which complicates the optimization. \n\n To address the above-mentioned issues, we present , a principled framework for accelerating a broad class of discrete diffusion models. In summary, our core contributions are as follows:\n % The core principle of this approach is to treat the pretrained teacher model as a fixed optimal solution to a denoising objective. The student distribution is then optimized to be the one for which this teacher is optimal.\n % is the first method, , that in a principled manner extends the framework to . In this regard, we:\n\n {1pt}\n {1pt}\n We theoretically establish the uniqueness of the global optimum for the IDLM objective, validating the correctness of the proposed optimization procedure ( ).\n\n We propose gradient-stable relaxations that enable effective training through (i) a generator and (ii) a principled mechanism for ( ).\n We empirically demonstrate that IDLM achieves competitive , matching the performance of 1024-step teacher models while requiring up to $64 $ fewer sampling steps ( ).",
  "sec/preliminaries.tex": "% !TEX root = ../main.tex\n\n We represent scalar discrete random variables over $N$ categories as one-hot column vectors, with ${ = \\{x \\{0,1\\}^N _{i=1}^N x_i = 1\\}}$ denoting the set of such vectors. We further define the probability simplex $ = \\{ z ^N z 0, z, = 1 \\}$ as the convex hull of $ $, where $ ^N$ denotes the all-ones vector. Additionally, let $I ^{N N}$ be the identity matrix. The matrix-weighted inner product is defined as $ a, b _Q = a^ Q b$, and we omit the subscript when $Q = I$. For absorbing diffusion process we define special mask token $m $ such that $m_N = 1$.\n\n We start by recalling the Diffusion Language Models (DLMs; ). Consider a distribution $p^*(x_0) $ over $ $. DLMs aim to generate samples from $p^*(x_0)$ by constructing a transformation process from an initial tractable distribution, based on predefined .\n\n A forward discrete diffusion process is defined by a family of time-dependent distributions $p_t(x_t) ,$ evolving over the interval $t [0, 1].$ This evolution follows a continuous-time Markov chain defined by a linear ordinary differential equation~ :\n % {Forward Diffusion}\n % \n % {dt} = Q_t p_t, p_0 = p^*,\n % \n % \n % \n\n {dt} = Q_t p_t, p_0 = p^*,\n\n where $Q_t ^{N N}$ are the diffusion matrices and have nonnegative non-diagonal entries and columns which sum to zero, ensuring valid probability dynamics. These matrices are typically chosen in the form $Q_t = _t Q$, where $Q$ is a fixed base matrix that defines the structure of the diffusion process, and $ _t$ is a time-dependent scalar that controls the diffusion schedule. With an appropriate choice of $Q$ and scheduling function $ _t$, the distribution $p_t$ converges to a tractable limiting distribution $ (x_1) $ as $t 1$.\n\n For a fixed initial state $x_0 p^*(x_0)$, the forward diffusion process defined in equation~ induces a family of conditional distributions over intermediate states $x_t $.\n {Conditional Distribution}\n\n % p_{t 0}(x_t x_0) = x_t, x_0 _{ ( _t Q )}, \n % \n % \\\\\n p_{t 0}(x_t x_0) = (x_t; ( _t Q )x_0 ), \n\n where, $ ( _t Q )$ denotes the matrix exponential, and $ _t _{0}^{t} _s \\, ds$ is the cumulative diffusion schedule. \n % Importantly, for any $x_0$, the matrix\u2013vector product $ ! ( _t Q ) x_0$ yields a probability vector whose entries sum to one, thereby ensuring a valid probability distribution.\n % For one-hot vectors $x_0, x_t $ such that $[x_0]_j = 1$ and $[x_t]_i = 1$, the conditional distribution $p_{t 0}(x_t x_0)$ corresponds to the just $(i, j)$-th entry of the matrix exponential, i.e.,$$ p_{t 0}(x_t x_0) = ( _t Q )_{i, j},$$ which corresponds to the formulation for conditional distributions in~ . \n % However, for our purposes, it is necessary to generalize this definition to accommodate any $x_0 $. For details on this relaxation, we refer the reader to ( ). Accordingly, we adopt the generalized formulation provided in Equation~ .\n\n The forward process transforms the unknown distribution $p^*$ into a known, tractable distribution $ $. The goal is to reverse this process backward in time to recover $p^*$ from $ $. This leads to the backward diffusion process, which is given by backward diffusion matrix $ _t$:\n % {Backward Diffusion}\n % \n % {dp_{1-t}}{dt} = _{1-t} \\, p_{1-t}, p_T = .\n % \n % \n % \n %\n\n {dp_{1-t}}{dt} = _{1-t} \\, p_{1-t}, p_1 = . \n\n %\n If the backward diffusion matrix $ _t$ is known, this process can be simulated using numerical methods such as Euler's~ .\n Consequently, the primary challenge is to estimate $ _t.$\n It can be derived from the forward diffusion matrix $Q_t$ using the following relationship~ :\n\n y, x _{ _{t}} = {p_t(x)} x, y _{Q_t}, \n x, x _{ _{t}} = - _{y x} y, x _{ _{t}},\n\n where $y, x $. The main challenge is to compute the ratio $ {p_t(x)}$, which depends on unknown intermediate distributions and cannot be computed directly. However, it can be estimated using the Concrete Score Matching approach.\n\n % As this ratio is generally intractable, a tractable alternative is used in the loss function via a conditional score function defined:\n % \n % \n % s(x, t x_0), y = {p_{t 0}(y x_0)}{p_{t 0}(x x_0)}.\n % \n % Importantly, the marginal ratio $ {p_t(x)}$ can be expressed as the conditional expectation of this score under the posterior, namely $ {p_t(x)} = _{x_0 p_{0 t}(x_0 x)} s(x, t x_0), y $.\n % The objective of the Concrete Score Matching framework~ is to approximate the ratio of marginal probabilities $ {p_t(x)}$.\n % propose to learn a score function model $ [0, T] _{+}^N$ using the (SEDD) loss in order to approximate it, which is expressed for data point $x_0 p^*(x_0)$ as:\n The goal of the Concrete Score Matching~ is to approximate the ratio of marginal probabilities $ {p_t(x)}$. To achieve this, propose learning a score function $ [0, 1] _{+}^N$ using the (SEDD) loss. This objective, defined for a data point $x_0 p^*(x_0)$, is given by:\n {SEDD Loss, $ _{ }( , x_0) =$}\n\n _0^1 _{p_{t|0}(x_t|x_0)} dt,\n\n where $ _{x_ty}$ is positive weighting function.\n show that the minimizer for the data distribution $p^*(x_0)$: \n \\[\n s^*= _{ } _{p^*(x_0)} \n \\] \n recovers the desired ratio of marginal probabilities $ {p_t(x)}.$ \n\n However, as noted in~ , applying this formulation to large-scale settings (e.g., GPT-2 with $N = 50257$ tokens) is practically challenging, as storing and computing all edge weights in the transition matrix $Q_t$ becomes infeasible. To address this, subsequent work has focused on specific classes of diffusion processes with simplified transition structures, reducing computational overhead while improving numerical stability and model efficiency.\n %\n % Additionally, showed that the minimizer for the whole data distribution $p^*(x_0)$\n % $$\n % s^* = _{ } _{p^*(x_0)} _{ }( , x_0)\n % $$ \n % recovers the true probability ratio, satisfying \n % $$\n % s^*(x_t, t),y ~=~ _{p_{0 t}(x_0 x_t)} s(x_t, t \\! \\! x_0),y ~=~ {p_t(x_t)}.\n % $$\n\n% Recent works~ use an alternative parameterization of the model using a function $ _0 [0, T] $, which approximates $x_0$. In this case, the score function can be expressed as $s(x, t)_y = {p_{t 0}(y _0(x, t))}{p_{t 0}(x _0(x, t))}$ {When $ _0 $, the conditional distribution is given by $p_{t 0}(x_t _0) = ( ( _t Q ) _0 )[x_t]$, where $ ( _t Q ) _0$ denotes the matrix\u2013vector product, and $[ ]$ selects the $x_t$-th component of the resulting vector. This is a natural extension of the conditional distribution defined in equation~ .}~ . and~ show that with specific choices of the forward matrix $Q_t$, the process becomes either absorbing (MDLM) or uniform (UDLM). In these cases, the loss in equation~ reduces to the (M/U)DLM loss:\n% \n% : \\\\\n% _{ }( _0, p) = \n% T _{x_0, x_t, t} _{y x_t} Q_t(x_t, y)\\, g_{ }( _0), \\\\\n% x_0 p(x_0), x_t p_{t 0}(x_t x_0), t ([0, T]), \n% \n% where $g_{ }$ is defined in equations~ and~ for MDLM and UDLM, respectively. For a detailed discussion on how this formulation recovers the structure of the original (M/U)DLM losses, we refer the reader to Appendix~ .\n\n% introduced Duo formulation, which demonstrates that applying the pushforward of the $ $ operator to a Gaussian process yields a uniform diffusion process. This observation motivates a relaxation of the model input domain, extending the parameterization to $ _0 [0, T] $. As a result, equation~ undergoes a minor modification. However, the overall structure of the loss functional remains unchanged. For a detailed discussion of this formulation, we refer the reader to Appendix~ .\n\n % Another DLMs works concentrated on two specific types of processes and another model parameterizations. The first an important considered diffusion process is the . \n % where the terminal distribution is a delta distribution centered on a mask token, i.e., $ = m$. This is achieved by selecting a specific base matrix $Q_{ }$ (see Appendix~ ), yielding the diffusion matrix $Q_t = { _t}(I - ^ )$.\n % A prominent example of this process is (MDLM; ), where the authors adopt an alternative model parameterization $ _0 [0, T] ,$ leading to the following loss for $x_0 p^*(x_0)$: \n % that approximates the posterior distribution $p_{0 t}(x_0 x_t)$. Since the posterior distribution is also intractable, the authors instead use an explicit $x_0$ in the loss function. Importantly, the objective also can be reformulated in terms of a conditional expectation over a tractable quantity, {namely $p_{0 t}(x_0 x_t) = _{p_{0 t}(x_0 x_t)} $.}\n % Substituting this into the SEDD loss~ yields the following form (for $x_0 p^*(x_0)$):\n Previous work on DLMs has primarily focused on two particular classes of processes. The first is the (see Appendix~ for further details). A prominent example of that is the (MDLM; ), in which the authors, rather than directly parameterizing the probability ratios $ {p_t(x)}$, follow the strategy of~ and instead learn the reverse conditional density $p_{0 t}(x_0 x_t)$. Accordingly, they adopt an alternative parameterization $ _0 [0, 1] $, which yields the following training objective for a fixed data point $x_0~ ~p^*(x_0)$:\n {MDLM Loss, $ _{ }( _0, x_0) =$}\n\n _0^1 \\, _{p_{t|0}(x_t x_0)} dt,\n\n %\n where $ _{t}$ is positive weighting function and $ _0(x_t, t)$ is element-wise logarithm of $ _0(x_t, t)$. Note that in the original work, the authors use the notation $ _0(x_t, t), x_0 $, which is equivalent. Moreover, show that this loss corresponds to the negative evidence lower bound (NELBO). As a result, at optimality, the learned model on whole data distribution $p^*(x_0)$: \n \\[\n x_0^* = _{ _0} _{p^*(x_0)} \n \\]\n recovers the target reverse conditional density $p_{0 t}(x_0 x_t).$\n % Moreover, shows that the MDLM loss~ corresponds to the negative evidence lower bound (NELBO) in the limit as $T $. Consequently, the optimal solution of this objective for the data distribution $p^*(x_0)$\n % \n % x_0^* = _{ _0} _{p^*(x_0)} _{ }( _0, x_0)\n % \n % \n % satisfies $x_0^*(x_t, t) = _{p_{0 t}(x_0 x_t)} = p_{0 t}(x_0 x_t)$.\n\n % Another key example is the . \n % where the terminal distribution is uniform, i.e., $ = {N} $. This is also achieved by choosing a specific base matrix $Q_{ }$ (see Appendix~ ), yielding the diffusion matrix $Q_t = - {N _t}( ^{ } - N I)$. \n % A representative case of this process is the (UDLM; ), which adopts the same parameterization as MDLM. This leads to the simplified form of the SEDD loss~ {For brevity, we omit the arguments and write $ _0 = _0(x_t, t)$.} for one data point $x_0 p^*(x_0)$:\n % {UDLM Loss, $ _{ }( _0, x_0) =$}\n % \n % \n % \n % _0^1 _{p_{t|0}(x_t|x_0)} dt.\n % \n % \n % %\n % further introduced the (Duo) framework, connecting Gaussian and uniform processes. This leads to an extended parameterization $ _0 [0, T] $ and a minor modification of the UDLM loss~ , while preserving its overall structure.\n % Additionally, show that the UDLM loss in equation~ also corresponds to the NELBO in the limit as $T $. Consequently, the optimal solution for the dadistributionion $p^*(x_0)$:\n % \n % x_0^* = _{ _0} _{p^*(x_0)} _{ }( _0, p^*) \n % satisfies $x_0^*(x_t, t) = _{p_{0 t}(x_0 x_t)} = p_{0 t}(x_0 x_t)$.\n Another important case is the (see Appendix~ for further details). Two notable models have been proposed in this setting: the (UDLM; ) and its extension, (Duo; ). UDLM adopts the same model parameterization and target function as MDLM, and introduces the loss for a data point $x_0 p^*(x_0)$ as $ _{ }( _0,x_0) =$\n\n _0^1 _{p_{t|0}(x_t x_0)} g (x_t, x_0, _0(x_t, t) ) dt,\n\n where the function $g (x_t, x_0, _0(x_t, t) ) =$\n\n _{t} ( {p_{t 0}(x_t x_0)} - {p_{t 0}(x_t _0)} \n \\\\ \n - _{y x_t} {p_{t 0}(y x_0)}{p_{t 0}(x_t x_0)} {p_{t 0}(x_t _0) \\, p_{t 0}(y x_0)}{p_{t 0}(y _0) \\, p_{t 0}(x_t x_0)} ), \n\n with $ _t$ denoting a positive weighting function. For clarity, we slightly abuse notation by omitting the model's input in $ _0$, assuming $ _0 = _0(x_t, t)$ within the expression.\n\n Duo further introduces an objective with reduced variance. To achieve it the authors first introduce distribution:\n\n _{t 0}(w_t x_0) = ( _t x_0, (1- _t^2)I), \n\n where $ _t$ denotes a rescaled diffusion schedule. Then they show that for $w_t _{t 0}(w_t x_0)$ and $x_t(w_t) = (w_t)$:\n \\[\n x_t(w_t) p_{t|0}(x_t x_0), \n \\]\n where $ (w_t)$ denotes a one-hot vector whose index corresponds to the maximum value in $w_t$. Based on this, they propose reducing the variance of the objective by substituting the discrete model input $x_t(w_t)$ with soft approximation:\n\n x^{ }_{t}(w_t) = ({w_t}/{ } ). \n\n % Note, that $ _t(w_t) = ( _t^{ }) = ( ( { }))$.\n This relaxation leads to an extended model parameterization $ _0 [0, 1] $. The resulting loss function for a data point $x_0 p^*(x_0)$ is given by:\n {Duo Loss, $ _{ }( _0, x_0) =$}\n\n _0^1 _{ _{t 0}(w_t x_0)} dt.\n\n %\n Moreover, shows that, in the limit as $ 0^{+}$, this loss corresponds to the NELBO of UDLM. Consequently, at optimality and in this limit, the learned model on whole data distribution $p^*(x_0)$:\n \\[\n x_0^* = _{ _0} _{p^*(x_0)} ( _0, x_0)\n \\]\n recovers the reverse conditional distribution $p_{0 t}(x_0 x_t).$\n\n In practical applications, we are interested in generating sequences of some length. A direct extension of the diffusion process to this setting would require modeling over an exponentially large space, resulting in a diffusion matrix of intractable size. To overcome this limitation, following~ , we consider diffusion processes defined by sparse diffusion matrices that perturb tokens independently across sequence positions. Under this assumption, the forward diffusion matrix factorizes across tokens, yielding both a factorized conditional distribution and, crucially, a loss function that decomposes as a sum of token-wise losses. As a result, the per-token objectives in equations~ , , and~ can be directly applied to sequence modeling.\n% \n% p_{t 0}(x_t^{1:L} x_0^{1:L}) = _{i=1}^L p_{t 0}(x_t^{i} x_0^{i}), \n% \n% This structure induces a corresponding model parameterization\n% $ ^L [0, T] _{+}^{N L}$,\n% and yields the following SEDD loss:\n% \n% : \\\\\n% _{ }( , p) =\n% T _{x_0^{1:L}, x_t^{1:L}, t}\n% _{i=1}^L _{y x_t^i}\n% Q_t(x_t^i, y)\\, g_{ }( ), \\\\\n% x_0^{1:L} p(x_0^{1:L}), \n% x_t^{1:L} p_{t 0}(x_t^{1:L} x_0^{1:L}), \n% t ([0, T]). \n% \n% The same factorization extends naturally to the (M/U)DLM setting, resulting in the model parameterization $ _0 ^L [0, T] ^L$. Analogously, for the Duo model the parameterization takes the form $ _0 ^L [0, T] ^L$. Under this formulation, the loss function is given by:\n% \n% : \\\\\n% _{ }( _0, p) =\n% T _{x_0^{1:L}, x_t^{1:L}, t}\n% _{i=1}^L _{y x_t^i}\n% Q_t(x_t^i, y)\\, g_{ }( _0), \\\\\n% x_0^{1:L} p(x_0^{1:L}), \n% x_t^{1:L} p_{t 0}(x_t^{1:L} x_0^{1:L}), \n% t ([0, T]). \n% \n\n Diffusion language models share the same problem of long iterative inference as continuous state space diffusion models. One of the primary approaches to mitigating this issue in continuous state space models is the use of a procedure known as . This technique involves training a new model, referred to as the , to approximate the sampling behavior of the original diffusion model, termed the , but with significantly fewer inference steps. For continuous space diffusion models, a huge number of different distillation techniques were developed~ . A particularly prominent class of these methods involves the integration of an auxiliary diffusion model, referred to as the model, into the training process~ .\n\n % Although discrete diffusion models have demonstrated strong performance in text generation tasks, their reliance on multi-step inference substantially limits their practical applicability. In contrast, a large body of work has focused on accelerating diffusion models in continuous spaces, among which distillation-based approaches have emerged as particularly effective~ . These approaches aim to distill a pre-trained multi-step diffusion model into a few-step student generator capable of producing high-quality samples. A notable instantiation of this approach involves training an auxiliary or \u201cfake\u201d model as part of the distillation process to support the training of the few-step student generator. While these methods were initially developed for different classes of models, they can be unified under the broader framework of . This approach was first introduced in the Inverse Bridge Matching Distillation framework~ , where the authors derived a distillation objective based on an inverse Kullback\u2013Leibler (KL) divergence. Subsequently, demonstrated that several prior works~ also implicitly can be described within this framework. Nevertheless, these connections have thus far been explored primarily in the context of continuous-space diffusion models and presented as auxiliary theoretical insights rather than as core methodological principles. In contrast, our work explicitly adopts the inverse distillation perspective and extends it to the discrete diffusion setting, positioning it as a central component of our approach.\n %\n We focus our attention on the distillation using an auxiliary fake model. One of the branches of this work is the direction of \n % In this work, we build upon the concept of \n , originally introduced in the Inverse Bridge Matching Distillation framework~ for a special type of diffusion models called ``Diffusion Bridge Models'' and further developed by~ for the general case of flow and diffusion models. \n\n Let $p^*(x_0)$ denote the data distribution and let $ _{ }( , x_0)$ be the per-example training loss\n used to fit a continuous diffusion model $f$ from data.\n The pretrained model is defined as the optimizer of\n\n f^* = _{ } \\ _{p^*(x_0)} .\n\n% One important instance of is \n% , where one matches a model field $ f(x_t,t)$ to the conditional field induced by\n% the forward process started at $x_0$:\n% \n% L_d(f; x_0)\n% := _{t [0,T]}\\,\n% _{x_t p_{t|0}( |x_0)}\n% \\| f(x_t,t) - f_{p_0}(x_t,t\\,|\\,x_0) \\|^2 .\n% \n% \n% In this case one can take $L_{ } L_d$.\n\n Inverse distillation optimizes a $p_ (x_0)$ parametrized by one or few step generator network such that the fixed teacher $f^*$ remains optimal under $p_ $. Specifically, inverse distillation considers objective $ _{ }( ) :=$\n\n \\!\\!\\! _{p_{ }(x_0)} \\! - _{ } _{p_{ }(x_0)} \\! ,\n\n % {Inverse Distillation, $ _{ }( ) =$}\n % \n % \\!\\!\\! _{p_{ }(x_0)} \\! - _{ } _{p_{ }(x_0)} \\! \\!, \\!\\!\n % \n % \n % \n % _{ }( )\n % :=\n % _{x_0 p_ } \n % \\;-\\;\n % _f \\ _{x_0 p_ } ,\n % \n % \n where the inner minimizer is the model trained on samples from $p_ $. \n By construction, the inverse loss satisfies $ _{ }( ) 0$ and for $p_ (x_0) = p^*(x_0)$ the minimum value $0$ is attained. However, this formulation does not, in general, guarantee the uniqueness of the minimizer meaning that optimization may converge to suboptimal solutions.\n % By definition, $ _{ }( ) 0$, and for $p_ (x_0) = p^*(x_0)$ the minimum value $0$ is attained.\n\n % Since , \n % We focus our attention on the distillation using an auxiliary model. One of the branches of this work is the direction of \n % % In this work, we build upon the concept of \n % , originally introduced in the Inverse Bridge Matching Distillation framework~ for a special type of diffusion models called \"Diffusion Bridge Models\" and further developed by~ for the general case of flow and diffusion models. \n\n % However, prior studies have primarily explored this idea in the context of continuous diffusion models, while our work explicitly extends the inverse distillation to the discrete setting.\n % This paradigm considers a unified learning objective for flows and diffusions based on the unification of conditional score and conditional flow matchings. Consider a (specifically D-dimensional real space $ ^D$) probability path $\\{p_t \\}_{t 0, T}$ on the time interval $[0, T]$ that transforms data distribution $p^*_0$ into noise distribution $p_T$. The path itself is built as a mixture of simple conditional paths $\\{p_t( |x_t) \\}_{t [0, T]}$ conditioned on samples $x_0 p(x_0)$:\n % \n % p_t(x_t) = _{ ^D} p_t(x_t|x_0)p(x_0)dx_0.\n % \n % \n % In turn, with each conditional path $p_t(x_t|x_0)$ there is associated a function $f^{p^0}( |x_0)$ which recovers it (i.e., score, or ODE/SDE drift functions of diffusion started from data point $x_0$). To fit a function $f^{p_0}$ which recovers $p^*(x_0)$ from the noise $p_T$ the authors propose a unified matching loss:\n % { , $ _{ } =$}\n % \n % _{p(x_t| x_0)} \\| (x_t) - _t^{p_0}(x_t|x_0)\\|^2\n % \n % \n % The model $f^*$ trained by the minimization of this loss is called model:\n % \n % f^*~=~ _{ } _{p^*(x_0)} .\n % \n % \n % This paradigm considers a parametric family of optimization problems defined by an objective function $ ( , )$, where $ $ denotes the optimization variable and $ $ represents the problem parameters. We define the as finding the optimal solution $f^*$ for given parameters $ $, that is, $f^*~=~ _{ } ( , )$. \n % Then the authors propose a general way of distilling this model into a one-step generator $G_{ }$, which parametrizes the distribution $p_{ }(x_0)$ using the following objective:\n % Conversely, the inverse optimization problem assumes access to a desired solution $f^*$ and aims to recover parameters $ ^*$ such that $f^*$ solves the corresponding forward problem $f^* = _{ } ( , ^*)$. A standard approach to solving the inverse problem is to minimize the following objective~ :\n % where model $ $ is called model. By construction, the inverse loss satisfies $ _{ }( ) 0$. Furthermore, for the case of $p_{ }(x_0) = p^*(x_0)$ (i.e., $G_{ }$ produces a data distribution in one step), the loss attains its minimum value of 0. \n\n % {Write gradient formula for inverse loss and say that everything is easy in case of continious diffusion. Or do it in the method section?}\n\n While such a general description of the inverse distillation was proposed in~ the particular cases of this distillation were introduced before, specifically for continuous diffusion (Score identity Distillation, SiD) in~ and specifically for continuous flows (Flow Generator Matching, FGM) in~ . \n\n In space, the model distribution $p_ $ is typically represented via a few-step generator and reparameterized, allowing the optimization of~ via backpropagation through sampled trajectories. In contrast, extending this approach to the domain presents several challenges, stemming from the inherent characteristics of discrete spaces. These include regarding the validity of the training objective, due to the non-uniqueness of its minimizers, as well as related to the non-differentiability of discrete sampling. Our objective is to extend the inverse distillation framework to the widely studied class of by addressing both of these challenges, as discussed in .\n % While the loss attains its minimum value of $0$ at the trivial solution $ = ^*$, this formulation does not guarantee the uniqueness of the solution. As a result, the optimization process may converge to suboptimal solutions.\n\n % In this work, we adopt the framework~ as the foundation for our main method. This paradigm considers a parametric family of optimization problems defined by an objective function $ ( , )$, where $ $ denotes the optimization variable and $ $ represents the problem parameters. The forward problem seeks the optimal solution $f^*$ for a given set of parameters $ $, i.e., $f^* = _{ } ( , )$. Conversely, the inverse optimization problem assumes access to a desired solution $f^*$ and aims to recover parameters $ ^*$ such that $f^*$ solves the corresponding forward problem $f^* = _{ } ( , ^*)$. A standard approach to solving the inverse problem is to minimize the following objective:\n % {Inverse Optimization Objective}\n % \n % \n % _{ }( ) = (f^*, ) - _{ } ( , ).\n % \n % \n % %\n % By construction, the inverse loss satisfies $ _{ }( ) 0$. While the loss attains its minimum value of $0$ at the trivial solution $ = ^*$, this formulation does not guarantee the uniqueness of the solution. As a result, the optimization process may converge to suboptimal solutions.\n\n % It is worth noting that establish a connection between their proposed loss function and an inverse optimization formulation. However, this connection is developed in the context of continuous-space diffusion models and is presented primarily as an auxiliary theoretical insight rather than a central methodological component. In contrast, our work builds directly upon this inverse optimization perspective and extends it to the setting of discrete diffusion models.",
  "sec/method.tex": "% !TEX root = ../main.tex\n\n In this section, we present our core methodology for distilling a broad class of Diffusion Language Models into a few-step generator. Firstly, we provide the theoretical foundations supporting the extension of the Inverse Distillation framework to the discrete domain ( ). We then address the practical challenges arising from the non-smooth nature of discrete spaces ( ). Lastly, we outline key implementation details ( ).\n % However, a key challenge lies in the fact that existing training losses across prior models are formulated using different objective forms and parameterizations. \n % To enable a unified application of Inverse Distillation, we first establish a general functional formulation of training objectives in ~ and relate it to previous work. \n % Building upon this foundation, \n % We introduce our Inverse Distillation method in ( ), followed by a discussion of practical implementation details in ( ). \n\n % All considered frameworks of discrete diffusion models (SEDD, MDLM, DUO) train either denoiser $ _0(x_t, t)$ or score function $ (x_t, t)$ using according loss function for it.\n % The methods SEDD~ , MDLM~ , and Duo~ propose training a model parameterized either as a denoising function $ _0(x_t, t)$ or as a score function $ (x_t, t)$. To present the distillation framework in a unified manner, we introduce the notation $ (x_t, t)$ to represent a generic parameterized function, which may correspond to either $ $ or $ _0$.\n % and call it .\n % following standard notation of diffusion distillation papers. \n % In the same way we denote $ _{ }$ as functional using to train these models, i.e. either $ _{ }, _{ }, _{ }$. Thus, diffusion language models consider problem:\n\n SEDD~ , MDLM~ , and Duo~ introduce different training objectives $\\{ _{ }, _{ }, _{ }\\}$ \n and model parameterizations $\\{ (x_t,t), _0(x_t,t)\\}$.\n To describe the distillation framework in a unified fashion, we use $ \\{ _{ }, _{ }, _{ }\\}$ to denote the diffusion training objective and introduce $ (x_t,t) \\{ (x_t,t), _0(x_t,t)\\}$ as a parameterized function encompassing the relevant model parameterizations. Following the standard notation commonly adopted in diffusion distillation literature, we define the model as:\n\n f^* = _{ } _{p^*(x_0)} .\n\n % To model a complex data distribution $p^*(x_0)$, one typically trains a teacher model $f^* = _{ } _{ }( , p^*)$ using objective for teacher training~ , and then generates samples via a multi-step sampling procedure~ . \n% \n\n To address the issue of slow inference, we extend the Inverse Distillation framework, originally developed for continuous diffusion models, to the domain of Diffusion Language Models.\n % To do so, we need: \n % \n % (a) propose a new distillation objective for discrete diffusion models .\n % (b) prove that the minimization of this objective leads to a model that correctly models data distribution (Theorem~ ).\n % (c) propose a practical procedure to optimize this new objective ( ~ ).\n % \n % In this section, we cover theoretical aspects, i.e., points (a) and (b), while practical aspects are covered in ~ . \n Specifically, we consider a model defined by a generator ${G_{ } }$, parameterized by parameters $ $, which transforms latent variables from a latent space $ $ to a discrete output space $ $. The latent space $ $ is equipped with a tractable prior distribution $p_{ }$, inducing a distribution $p_{ }$ over $ $ via the mapping $G_{ }$. Then, following the logic of the continuous case, we propose to consider the following objective for the discrete diffusion:\n % We start by considering the following objective, similar to the continuous case:\n\n % To avoid the time-consuming sampling, we propose to train a student generator $G_{ } $, parameterized by $ $, to approximate $p^*(x_0)$ directly. The generator defines a distribution $p^ $ by pushing forward a tractable reference distribution $p^{ }$ over the latent space $ $, {i.e., $p^ = G_ \\# p^{ }$}. Our key idea is to frame the training of the student generator as an instance of inverse distillation, where the parameters are represented as distributions $p^ $, and the goal is to find a distribution $p^{ ^*}$ for which the solution to the forward optimization problem coincides with the teacher model, i.e., $f^* = _{ } _{ }( , p^{ ^*})$. Applying the inverse optimization objective in equation~ yields the following (IDLM) loss:\n {IDLM loss, $ _{ }( ) =$}\n\n \\!\\!\\!\\! _{p_{ }(x_0)} \\! \n - _{ }\\! _{p_{ }(x_0)}\\! ,\\!\n\n where the second term includes training of model using the same loss as the teacher model, but with data $p_{ }(x_0)$ produced by the generator $G_{ }$.\n\n However, simply formulating this objective in a manner analogous to the distillation loss used in continuous diffusion models does not guarantee the validity of its solutions. While the loss function admits a trivial minimizer $ ^*$ satisfying $p_{ ^*} = p^*$, the absence of a uniqueness guarantee implies that the optimization procedure may converge to suboptimal solutions. Consequently, it is essential to establish that the {minimizer recovers the true data distribution, i.e., ${p_{ ^*} = p^*}$}. To address this, we present the following theorem. \n\n % However, . While the loss function admits a trivial minimizer $ ^*$ satisfying $p^{ ^*} = p^*$, the absence of a uniqueness guarantee implies that the optimization procedure may converge to suboptimal solutions. Consequently, it is essential to establish that the {minimizer recovers the true data distribution, i.e., ${p_{ }^* = p^*}$}. This condition constitutes a key theoretical requirement for validating the soundness of the proposed objective. To address this, we present the following theorem.\n\n % While this loss admits a trivial solution at $ ^*$ such that $p^{ ^*} = p^*$, the uniqueness of this solution is not guaranteed. As a result, optimization may converge to suboptimal solutions. To address this, the following theorem establishes a connection between $ _{ }$ and the KL divergence $ _{ }$.\n [Unique solution]\n\n For the SEDD~ , MDLM~ , and Duo~ (in the limit as $ 0^{+}$) objectives the IDLM loss defined in equation~ satisfies\n\n _{ }( ) _{ }(p_{ } p^*) 0 \n\n and achieves its minimum (zero) if and only if the model distribution matches the target distribution\n\n _{ }( ) = 0 p_{ } = p^*. \n\n %\n We give the proof in Appendix~ .\n\n% \n\n% We define the generator $G_ : $ as a deterministic map designed to transport a tractable\n% reference measure onto the geometry of the probability simplex. Here, $ $ represents the continuous latent space\u2014typically $ ^d$ equipped with a standard Gaussian measure $p^ $\u2014and $ $ denotes the probability simplex in $ ^N$, representing the set of valid probability vectors over $N$ tokens.\n\n% This mapping induces a model distribution $p^ $ on the simplex, characterized as the pushforward of the latent prior. Mathematically, this is expressed as:\n% $$\n% p^ = (G_ )_\\# p^ .\n% $$\n% Here, the notation $(G_ )_\\#$ represents the pushforward of the measure $p^ $ by the measurable map $G_ $. This operator transports probability mass from the latent space to the output space, ensuring that for any measurable set $A $, the induced probability is given by $p^ (A) = p^ (G_ ^{-1}(A))$.\n\n% A critical aspect of this framework is the continuous relaxation of the discrete data structure. While the true data distribution $p^*$ is supported strictly on the vertices $ $ (corresponding to discrete one-hot vectors), the generator $G_ $ maps latent points into the continuous *interior* of $ $. This relaxation is essential, as it renders the mapping differentiable with respect to the parameters $ $. In the context of Inverse Distillation, $G_ $ parameterizes the student distribution; the training objective $ _{ }( )$ is consequently formulated to minimize the discrepancy between diffusion trajectories generated by this relaxed induced measure $p^ $ and those arising from the true data distribution $p^*$.\n\n% [t]\n% \n% {Input} {Output}\n% {Teacher network $f^{*}$; \\\\\n% Forward Optimization Loss $ _{ }$; \\\\\n% Samples from target distribution $p^*(x_0)$; \\\\\n% Number of student iterations $K$;\n% }\n% {Learned generator $G_{ }$}\n\n% {func}{}{}\n% {$ (p^*):$}{\n% $x_0 p^*(x_0)$ \\\\\n% $t $ \\\\\n% $x_{t} p_{t 0}(x_{t} x_0)$ {Eq.~ }\n% $x_{t}, t$ \n% }{}\n% \n% $G_ (f^*)$ \n% $ (f^*)$\n\n% {\n% \n% $x_{t'}, t' (p^*)$ \\\\\n% $ _{ } _{ }( , G_{ }(x_{t'}, t'))$ {Eq.~ } \n% Update $ $ by using $ { _{ }}{ }$\n\n% \n% $x_{ }, (p^*)$ \\\\\n% $ _{ }( ) $ {Eq.~ } \n% Update $G_{ }$ by using $ { _{ }( )}{ }$\n% }\n% \n\n [ht!]\n\n % \n { The objective is to distill a pretrained Diffusion Language Model $f^*$, referred to as the , into a few-step generator $G_{ }$, referred to as the . To this end, we extend the concept of , originally developed for continuous diffusion models, to the discrete domain. The training procedure involves a nested optimization scheme: an auxiliary diffusion model $ $ (the model), is optimized using the same training loss as the teacher, but with data generated by the student $G_{ }$. Subsequently, the generator is updated using the IDLM objective introduced in ( ).}\n\n% \n% While the theoretical foundations of the method have been established, a direct implementation of the IDLM loss for discrete diffusion models is not feasible in practice due to two primary challenges:\n% \n% The IDLM objective is not differentiable since it includes distribution $p_{ }$ parametrized by a discrete generator $G_{ }$. \n% % gradients must pass through discrete sampling steps, $x_0 p^{ }(x_0)$ and $x_t p_{t 0}(x_t x_0)$.\n% Distilling teacher diffusion $f^*$ into a one-step generator $G_{ }$ for text generation, is practically challenging due to limited model capacity and the difficulty of learning a direct mapping from latent space to diverse, high-quality text.\n% \n% %\n% To address these issues, we first propose a solution to the gradient estimation challenge in Section~ . Then, motivated by the second limitation, we extend the IDLM framework to support multi-step generation in Section~ . Finally, we summarize the method in Section~ .\n\nAlthough we have theoretically established the validity of the proposed inverse distillation objective IDLM~ , the discrete nature of the output space $ $ continues to pose significant practical challenges for the gradient-based optimization. \nIn particular, the IDLM loss contains two primary bottlenecks. (i) First, the student generator ${G_{ } }$ produces outputs in the one-hot space $ $, which in practice necessitates the use of unstable techniques, such as the hard Gumbel-Softmax, thereby complicating gradient propagation. (ii) Second, samples $x_t p_{t 0}(x_t x_0)$ depend on $x_0$, and thus implicitly on $ $. This introduces an additional challenge, as differentiating through the sampling operation $ $ is nontrivial in the discrete setting. Below we mitigate these challenges and develop a feasible training procedure.\n\nTo address the first challenge, we relax the output space of the student generator from the discrete set $ $ to the probability simplex $ $, and accordingly redefine the generator as $G_{ } $. This relaxation enables smooth gradient flow but introduces a nontrivial question regarding the computation of the training objective, since the generator outputs no longer lie in the original discrete space. Specifically, under this reparameterization, the IDLM objective becomes:\n\n _{ }( )\n \\!=\\! _{p_{ }(x_0)}\\! \\!=\n \\\\\n = _{p_{ }(z)}\\! ,\n\nwhere $z p_{ }(z)$ denotes a latent variable. Since $x_0$ appears only within the teacher loss $ $, we must ensure that this loss remains computable when substituting discrete samples $x_0 $ with relaxed outputs $G_{ }(z) $. To this end, we note that all teacher losses under consideration $ \\{ _{ }, _{ }, _{ }\\}$ incorporate $x_0$ in one of two mathematically compatible forms: (i) through scalar products $ , x_0 $, or (ii) via the distribution $p_{t|0}( x_0)$, which can be expressed as a matrix-vector product:\n\n p_{t 0}( x_0) = ( _t Q )x_0.\n\n%\nConsequently, each of the considered teacher losses is mathematically well-defined not only for one-hot vectors from $ $ but also for any input from the probability simplex $ $. This reparameterization enables end-to-end training of the generator using standard differentiable operations, such as the $ $ function, thereby avoiding the need for unstable discrete relaxation methods like the hard Gumbel--Softmax.\n% We address the first challenge by relaxing the output space of the student generator from the discrete set $ $ to the probability simplex $ $, and accordingly redefining the generator as $G_{ } $. However, this relaxation raises nontrivial validity concerns, as the resulting outputs no longer lie in the original discrete domain. In particular, under this reparameterization, the IDLM objective becomes\n% \n% _{ }( )\n% = _{p_{ }(x_0)}\\! \n% \\\\\n% = _{p_{ }(z)}\\! ,\n% \n% \n% where $z p_{ }(z)$ denotes the latent variable. It is therefore necessary to ensure that replacing discrete samples $x_0 $ with their relaxed counterparts $G_{ }(z) $ preserves the validity of the teacher loss $ _{ }$.\n% $ _{ }( )$.\n% While we theoretically established validity of novel extended inverse distillation objective , the practical question of using this objectives remains. Specifically, it is not clear how to efficiently use gradient-based optimization methods for objective which includes sampling from the discrete $p_{ }(x_0)$.\n% We start from the differentiation of the $ _{ }$. \n% % We remind, that:\n% % $$\n% % x_0 = G_{ }(z), z p(z).\n% % $$\n% \n% _{ } _{ }( ) = _{ } _{x p_{ }} = \n% \n% \\\\\n% _{ } _{z p(z)} = \n% \n% \\\\\n% _{z p(z)} _{x_0=G_{ }(z)}\n% \n% \n% Since $G_ $ outputs discrete one-hot tokens, it is non-differentiable (or piece-wise constant), so the gradient $ {dG_{ }}{d }$ vanishes / is undefined. \n% To overcome this problem we use a simplex relaxation ${x_0=G_ (z) }$, i.e. parametrize the generator $G_{ }$ to output samples on the simplex.\n% Furthermore, \n% and extend $ _{ }$ to the simplex, enabling standard backpropagation.\n% The gradient $ _ _{ }( )$:\n% involves differentiating through the expectation over $x_0 p^ $ and $x_t p_{t|0}( | x_0)$. In the IDLM loss~ , two primary sources of gradient flow complexity arise from these sampling steps, both of which depend on the generator parameters $ $. Since gradients must propagate through both the distributions and their corresponding samples, this results in four critical gradient pathways:\n% \n% \n% \n% \n% _ _{ } &= { { p^ } { }}_{ } + { { x_0} { }}_{ } \\\\\n% & + { { p_{t|0}} { p_{t|0}}{ }}_{ } + { { x_t} { }}_{ }.\n% \n% \n% \n% {Term (I): Generator Reparameterization ($ { { p^{ }}{ }}$).}\n% We employ a reparameterization strategy by expressing {$p^{ } = G_{ } \\# p^{ }$}, where the generator $G_{ } $ maps latent variables to the discrete output space. Under this formulation, expectations with respect to $p^{ }$ can be rewritten as expectations over the latent distribution $p^{ }$ by setting $x_0 = G_{ }(z) $ for $z p^{ }(z)$. This reparameterization allows gradients to propagate through the deterministic mapping $G_{ }$ and eliminates the need to compute the gradient $ { p^{ }}{ }$ directly. Thus, Term (I) vanishes.\n% ($ { { }}$).}\n% While the generator parameterization $G_{ } $ is theoretically well founded, the discreteness of the output space $ $ poses challenges for gradient-based optimization. In particular, propagating gradients through discrete outputs requires approximation techniques such as the Gumbel--Softmax relaxation, which are often unstable and lead to high-variance gradient estimates.\n% \n% \n% To address this issue, \n% [Natural extention of ]\n% \n% Let $ $ denote the probability measure over trajectories of the diffusion process. We further denote by $ ^{ }$ the trajectory distribution corresponding to the generator induced distribution $p^{ }$, and by $ ^{*}$ the trajectory distribution corresponding to the target distribution $p^*$. Under this notation, for a broad class of Discrete Diffusion Models, the IDLM loss in equation~ is equivalent to the following KL divergence objective:\n% \n% _{ }( ) = _{ } ( ^{ } ^*).\n% \n% \n% Since the KL divergence admits a unique minimizer, this ensures the uniqueness of the solution and provides guarantees for recovering a valid result.\n% \n% In order to resolve this concern, we note that all considered DLMs objectives $ _{ } \\{ _{ }, _{ }, _{ }\\}$, includes $x_0$ either in the form of scalar products $ , x_0 $ or in the form of a conditional forward distribution $p_{t|0}( |x_0)$, which in turn represented as matrx-vector product:\n% \n% p_{t 0}( x_0) = ( _t Q )x_0 \n% \n% \n% Thus, all considered $ _{ }$ are mathematically correctly defined not only for one-hot vector products from $ $, but also for any vector on a probability simplex ${ .}$ \n% We use this observation and relax the output space of the generator from the discrete set $ $ to the probability simplex $ $, redefining the generator as $G_{ } $, i.e. $x_0 = G_{ }(z) $. The inverse loss $ _{ }$ is given by:\n% \n% _{ }( ) = _{p_{ }(x_0)} = \n% \n% \\\\\n% _{p(z)} \n% \n% % \\\\\n% % _{p(z)} _{x_0=G_{ }(z)},\n% % \n% \n% This reparametrized form allows to backpropagate through relaxed generator $G_{ }$ without using unstable hard Gumbel-Softmax techniques and just leverage stable $ $ function.\n% \n% _{ } _{ }( ) = _{ } _{p_{ }(x_0)} = \n% \n% \\\\\n% _{ } _{p(z)} = \n% \n% \\\\\n% _{p(z)} _{x_0=G_{ }(z)},\n% \n% \n% where $ _{x_0} _{ }$ is the gradient of the loss used for the teacher model over $x_0$. \n\nSince the second problem depends on the specific form of conditional distribution $p_{t 0}(x_t x_0)$, we analyze the relevant cases separately, focusing on the Duo and MDLM (SEDD with absorbing process) formulations.\n\n% . The objective in Duo is given in equation~ as:\n% Forward process sampling is performed in the following reparametrized form:\n% \n% x_t = \\! ( { _t x_0 + (1 - _t^2) }{ } ).\n% \n% for some small $ $ which is gradually decreased to zero and schedule parameters $ $. Thus, for small $ $ backpropagation through the $ _{ }$ is also performed using reparametrization trick:\n% \n% _0^1 _{ _{t 0}(w_t x_0)} g_{ }(h^{ }_{w_t},x_0, _0(h^{ }_{w_t}, t))dt, \n% \n% in this case we can use the standard reparametrization trick for gaussian distribution defining by\n% $$\n% w_t(x_0, ) = _t x_0 + {(1- _t^2)} ,\n% $$ \n% where $ ( 0, I)$ and we obtain:\n% \n% _0^1 _{ ( |0, I)} g_{ }(h^{ }_{w_t(x_0, )},x_0, _0(h^{ }_{w_t(x_0, )}, t))dt,\n% \n% thus eliminating the necessity of backpropagation through the sampling process of distribution in expectation, because $ ( 0, I)$ does not depend on $x_0$.\n\n The training objective in the Duo setting is given in equation~ as $ _{ }( _0, x_0) =$\n\n _0^1 _{ _{t 0}(w_t x_0)} dt. \n\nHere we can apply the standard reparameterization trick for Gaussians by expressing the latent variable $w_t$ as:\n\n w_t(x_0, ) = _t x_0 + {1 - _t^2} \\, ,\n\nwhere $ (0, I)$ is sampled independently of $x_0$. Substituting this into the objective yields $ _{ }( _0, x_0) =$\n\n _0^1 _{ (0, I)} \\, \\, dt,\n\nwhich eliminates the need to differentiate through the sampling operation itself, as the randomness is now isolated in $ $, which is independent of $x_0$. This reparameterization thus enables gradient-based optimization of the Duo objective.\n\n% . In this case there is no reparametrization for $p(x_t|x_0)$. However, we can leverage here the SUBS parameterization of MDLM model introduced in ~ . Specifically, for unmasked $x_t m$, it is known that: (a) the optimal denoiser is $ _0(x_t, t) = x_t$, (b) that $x_t = x_0$ since the forward process can only transform $x$ to mask vector $m$. Hence $ _0(x_t, t) = x_t = x_0$ and:\n% \n% _{ }(x_0, x_0) = 0.\n% \n% \n% All nonzero contributions to the loss occur when $x_t = m$, but in this case $x_t$ does not depend on $x_0$\n% for which conditional distribution $p_{t 0}(x_t x_0)$ reduces to a constant~ . \n% As a result, neither $p_{t 0}(x_t x_0)$ nor the sampled $x_t$ depends on $x_0$. \n% and there is no need to backpropagate through sampling of $p_{t|0}(x_t|x_0)$. Thus, one can write the objective $ _{ }( _0, G_{ }(z))$ as:\n% \n% _0^1 \\, _{p_{t|0}(x_t| (G_{ }(z)))} dt.\n% \n% \n% \n% _0^1 \\, _{p_{t|0}(x_t|x_0)} dt\n% \n% \n% Same is true for $ _{ }$ with absorbing process, since for absorbing process $ _ $ is equivalent to $ _{ }$.\n\n In the case of MDLM, the forward transition distribution $p(x_t x_0)$ does not admit a standard reparameterization. However, we can leverage the parameterization of the MDLM model introduced in~ . Specifically, for unmasked tokens (i.e., $x_t m$), the following properties hold: (a) the optimal denoiser satisfies $ _0(x_t, t) = x_t$, and (b) $x_t = x_0$, since the forward process either preserves the token or replaces it with a fixed mask token $m$. As a result, in this case, $ _0(x_t, t) = x_t = x_0$, and the loss becomes $ _{ }(x_0, x_0) = 0.$\nThus, all nonzero contributions to the loss arise from instances where $x_t = m$. In this case, $x_t$ is independent of $x_0$, as the mask token is fixed and does not result from a stochastic transformation conditioned on $x_0$. Consequently, there is no need to backpropagate through the sampling of $p_{t 0}(x_t x_0)$. This allows us to rewrite the loss as:\n\n _0^1 \\, _{p_{t 0}(x_t (G_{ }(z)))} dt,\n\nwhere $ ( )$ denotes the stop-gradient operator. This formulation enables efficient training without requiring differentiable sampling from the discrete forward process.\n\n% Thus, the original gradient of $ _{ }$ can be computed using automatic differentiation techniques.\n\n% To conclude the derivation of the gradient, we consider $ _{x_0} _{ }$ case by case:\n% This relaxation enables stable gradient-based optimization using the differentiable $ $ function. \n% Unlike stochastic relaxations that introduce variance, this analytical extension provides a deterministic path for gradients. \n\n% Teacher loss $ _{ }(x_0, f)$ given by under this relaxation naturally extends without any changes. Note, that all variants of teacher loss includes $x_0$ in one of three ways: through ${p_{t 0}(x_t x_0)}$, through matrxi-vector products (e.g. \n\n% Specifically, the conditional distribution ${p_{t 0}(x_t x_0)}$ in equation~ remains well defined, as it corresponds to a matrix-weighted inner product with $x_0 .$ \n\n% Teacher loss $ _{ }(x_0, f)$ given by under this relaxation naturally extends without any changes. Specifically, the conditional distribution ${p_{t 0}(x_t x_0)}$ in equation~ remains well defined, as it corresponds to a matrix-weighted inner product with $x_0 .$ Similarly, the conditional function $f(x_t, t x_0)$ remains well defined, as it either involves ratios of conditional distributions (in the SEDD case) or depends explicitly on $x_0$ (in the MDLM and UDLM settings).\n% Crucially, under this extension the teacher objective~ \n% remains well-defined , because it depends on $x_0$\n% only through matrix-vector products with $ x_0$.\n% Indeed, $p_{t|0}( x_0)$ is given by $K_t x_0$,\n% and the corresponding conditional targets admit the same property:\n% \n% the conditional score uses only ratios of entries of $K_t x_0$, e.g.\n% \\[\n% s(x_t,t x_0),y \n% = { x_t,K_t x_0 },\n% \\]\n% so it depends on $ x_0$ only via the vector $K_t x_0$;\n% the target is $x_0$ itself, which extends linearly to $ x_0 $,\n% turning the per-token loss into a cross-entropy,\n% \\[\n% ( x_0, x_0(x_t,t))\n% = - _{v=1}^N ( x_0)_v x_{0,v}(x_t,t)\n% = - x_0^ x_0(x_t,t).\n% \\]\n% \n\n% Similarly, the conditional function $f(x_t, t x_0)$ remains well defined, as it either involves ratios of conditional distributions (in the SEDD case) or depends explicitly on $x_0$ (in the MDLM and UDLM settings).\n% For the loss function, we must verify that it extends to $ _0 ^{V-1}$. For MDLM, the loss takes the form:\n% \n% \n% - _{v } ( _0)_v (m, t)_v = - _0^ (m, t),\n% \n% which is differentiable in $ _0$. Similar extensions hold for SEDD and UDLM.\n\n% Apart of computing $ {dG_{ }}{d }$ the gradient involves computation of $ _{x_0} _{ }(x_0, f)$:\n% \n% _{x_0} _0^1 \\!\\! _{p_{t|0}(x_t|x_0)} \\!\\! _{y x_t}\\! w_t _{F _t}\\! (f(x_t, t \\! \\! x_0), (x_t, t) ) \\!dt\n% \n% \n% The main obstacle of computing this gradient over $x_0$ is taking the gradient over expectation over $p_{t|0}(x_t|x_0)$. \n\n% {Terms (III) and (IV): Process-Specific Estimation ($ { { p_{t 0}}{ }}$ and $ { { }}$).}\n% The conditional distribution $p_{t 0}$ depends on the diffusion matrix and, consequently, on the specific diffusion process. This structure can be exploited to handle both gradient terms effectively. \n\n% \n% In the case of the for unmasked $x_t m$, it is known that: (a) the optimal denoiser is $ _0(x_t, t) = x_t$, (b) that $x_0 = x_t$ since the forward process can only transform $x$ to mask vector $m$. In this case:\n% \n% _0(x_t, t), x_0 = 0, _{ }( _0, x_0) = 0.\n% \n% \n% All nonzero contributions to the loss occur when $x_t = m$, for which conditional distribution $p_{t 0}(x_t x_0)$ reduces to a constant~ . As a result, neither $p_{t 0}(x_t x_0)$ nor the sampled $x_t$ depends on $x_0$. And $ _{x_0} _{ } =$\n% \n% _0^1 \\, _{p_{t|0}(x_t|x_0)} dt\n% \n% \n% Same is true for $ _{ }$ with absorbing process, since for absorbing process $ _ $ is equivalent to $ _{ }$. Thus, the original gradient of $ _{ }$ can be computed using automatic differentiation techniques.\n\n% \n% For the , we adopt the Diffusion Duality (Duo) formulation~ , which establishes a connection between Gaussian and uniform diffusion processes through a reparameterization of the diffusion schedule, denoted by $ _t = ^{-1}( _t)$. Under this formulation, the expectation over $p_{t 0}(x_t x_0)$ appearing in the loss can be replaced as an expectation over $ (0, I)$ via the change of variables $x_t = _{ 0^+} \\! ( { _t x_0 + (1 - _t^2) }{ } ).$\n% In practice, we fix $ $ to a sufficiently small value, which yields a numerically stable $ $ transformation and ensures that $x_t $. The gradient $ _{x_0} _{ }$ =:\n% \n% _{x_0}\n% \n% \n% Importantly, within the Duo framework, the teacher model is parameterized as $f [0, T] $, and therefore naturally accepts inputs from the simplex, allowing gradients to propagate through $x_t$ in a stable manner. {why we say it here and only for Duo?}\n\n % \n\n % \n % Summarizing everything we define the Relaxed IDLM loss, where to generalize both absorbing and uniform processes we use notation of conditional distribution $p_{t 0}$ and $x_t$ taking into account that in case of absorbing they do not depend on parameters and in case of uniform we use reparametrization trick, Therefore:\n % {Relaxed Forward Optimization Problem}\n % \n % \n % \n % & _{ }( , G_{ }) = T _{z,x_t,t} _{y x_t} w \\\\\n % & , \\\\\n % & z p^{ }(z), x_t p_{t 0}(x_t G_{ }(z)), t U([0, T]), \n % \n % \n\n Following prior distillation approaches that employ an auxiliary fake model during training~ , we initialize both the fake diffusion model and the student generator using the parameters of the pretrained teacher diffusion model.\n\n We observe that distilling a single-step generator for text generation remains practically challenging. This observation motivates us to follow prior works~ and extend the IDLM framework to support multistep generation. To enable few-step inference, and given that the student generator is initialized from the pretrained teacher model, we adopt the same latent space $ $ as for the teacher. Therefore, the generator is parameterized as $G_{ } [0, 1] $ in the SEDD and MDLM settings, and as $G_{ } [0, 1] $ in the Duo setting. To avoid notational ambiguity, we denote samples from the target data distribution as $ _0 p^*( _0)$. Under this parameterization, the generator receives as input either: (i) samples $x_{ } p_{ 0}(x_{ } _0)$ in the SEDD and MDLM settings, or (ii) soft embeddings $x_{ }^{ }(w_{ }( _0, ))$ in the Duo setting, where $ (0, I)$ and $ _0 p^*( _0)$ is drawn from the data distribution.\n This design enables the generator to leverage supervision from ground-truth data samples, which can lead to improved generation quality. As a result, the generator is capable of performing multistep inference in the same manner as the teacher model, by numerically integrating the reverse-time dynamics~ .\n\n % We observe that distilling a one-step generator for text generation remains practically challenging. this motivates us to follow~ and extend IDLM to support multi-step generation. To enable few-step generation and since we initialize the generator model from the teacher we use the same latent space and reparameterize the generator to reflect the teacher model and we align the training procedure of the generator with that of the teacher model. Specifically as $G_{ } [0, T] $ for SEDD and MDLM settings, or $G_{ } [0, T] $ in the Duo setting. Under this parameterization, the generator takes as input samples $x_{t} p_{t 0}(x_{t} x_0)$, where $x_0 p^*(x_0)$ is sampled from the data distribution. This setup allows the generator to leverage supervision from true data samples, potentially improving generation quality. Furthermore, the generator can be used in multi-step inference by numerically solving the reverse-time dynamics defined in equation~ .\n\n [t]\n\n \\\\[-2mm]\n\n { IDLM-SEDD ( ) matches SEDD generation quality and diversity with far fewer steps, reducing the number of steps from $1024 256$ ($4 $).\n GenPPL vs. sampling steps shows that IDLM\u2011SEDD consistently outperforms the original SEDD model across all sampling steps, without significantly sacrificing {entropy}.}\n\n [t]\n\n \\\\[-2mm]\n\n { IDLM-MDLM ( ) matches MDLM/SDTT generation quality and diversity with far fewer steps, reducing sampling steps for MDLM from $1024 16$ ($64 $). GenPPL vs. sampling steps shows that IDLM-MDLM performs best in the regime with high {entropy}.}\n\n [t]\n\n {6pt}\n\n {@{}cc@{}}\n &\n \\\\[-2mm]\n &\n \\\\[-1mm]\n (a) Ancestral sampling ($^{ }$) &\n (b) Greedy-Tail sampling ($^{ }$)\n\n { Under both Ancestral ($^{ }$) and Greedy\u2011Tail ($^{ }$) sampling, our distilled models maintain GenPPL and {entropy} while requiring significantly fewer sampling steps. Compared to the original Duo model, IDLM\u2011Duo ( ) reduces the steps from $1024$ to $16$ ($64 $), and IDLM\u2011DCD ( ) further reduces them to $8$ steps ($128 $) under Ancestral sampling and to $4$ steps ($256 $) under Greedy\u2011Tail sampling. GenPPL vs. sampling steps shows that IDLM-DCD achieves the lowest GenPPL across all sampling steps under both samplers, while maintaining comparable {entropy}. IDLM-Duo also improves upon Duo and achieves metrics comparable to Duo-DCD under Ancestral sampling, but exhibits a modest GenPPL gap relative to Duo-DCD under Greedy-Tail sampling in regime.}\n\n The proposed IDLM loss function involves an inner optimization with respect to the fake model. To approximate this nested optimization in practice, we adopt an alternating training scheme, following prior works~ . Specifically, training proceeds by alternating between two optimization steps.\n\n (i) $ $ while keeping the student generator $G_{ }$ fixed. In this step, the fake model is optimized using the teacher loss $ $ evaluated on samples generated by the student. Taking into account the multistep training procedure, for a fixed sample $x_{ } p_{ 0}(x_{ } _0)$ with $ _0 p^*( _0)$, the resulting optimization objective for updating the fake model is given by:\n\n { } { } \\\\\n _{ } = \\! ( , G_{ }(x_{ }, )). \n\n (ii) $G_{ }$ while keeping the fake model $ $ fixed. Considering the multistep training procedure, for a fixed sample $x_{ } p_{ 0}(x_{ } _0)$ with $ _0 p^*( _0)$, the IDLM loss~ can be instantiated as:\n\n { } { } \\\\\n _{ }( ) \\!=\\! (f^*, G_{ }(x_{ }, ))-\n \\! ( , G_{ }(x_{ }, )). \n\n In summary, Algorithm~ outlines the proposed procedure for distilling a pretrained DLM into a few-step generator using the IDLM objective. A visual illustration of the method is provided in Figure~ .",
  "sec/experiments.tex": "% !TEX root = ../main.tex\n\n This section demonstrates the applicability of our IDLM distillation method across various types of DLMs. To this end, we conduct experiments using of pretrained SEDD, MDLM, and Duo teacher models trained on OpenWebText (OWT; ). For evaluation, we follow the protocol of prior works~ and report GPT-2 Large~ generative perplexity (GenPPL) as well as average sequence entropy to measure diversity. Comprehensive results are summarized in Table~ . Additional for all evaluated methods are provided in Appendix~ . We also provide the text samples in Appendix~ . All implementations were developed in PyTorch, and the code will be published.\n% \n% \n% \n% \n\n To the best of our knowledge, no distillation methods have been developed specifically for the SEDD model. This section therefore focuses on demonstrating the effectiveness of our distillation approach in reducing the number of sampling steps required to achieve comparable generation quality of the teacher model. In the case of SEDD, only checkpoints for the absorbing-process variant are available, so we distill this version and refer to it simply as SEDD for clarity.\n As shown in Figure~ , our method, , consistently outperforms SEDD in terms of GenPPl across all sampling steps. Notably, IDLM-SEDD maintains high sequence entropy at low sampling steps. IDLM-SEDD achieves a $4 $ reduction in sampling steps, accelerating generation from $1024$ to $256$ steps, while preserving both GenPPL and output diversity.\n\n% \n% \n% \n% \n\n This section addresses two primary objectives: (1) demonstrating the feasibility of MDLM model distillation within our framework and (2) comparing with the SDTT~ a leading method for MDLM distillation. As shown in Figure~ , our method, , exhibits reduced entropy at large sampling steps, indicating lower diversity in that regime. However, in the low\u2013sampling-step regime, IDLM-MDLM achieves strong performance in terms of both GenPPL and entropy. In particular, IDLM-MDLM attains a $64 $ acceleration for both MDLM and SDTT, reducing the number of sampling steps from $1024$ to $16$ while preserving comparable generation quality and diversity metrics.\n\n% \n% \n% \n% \n\n Two related models have been proposed for uniform discrete diffusion: UDLM~ and its extension, Duo~ . As shown in the Duo paper, it achieves better generation quality than UDLM, so we focus our distillation experiments on Duo. Duo supports two sampling strategies, Ancestral and Greedy-Tail, which we denote by superscripts ($^{ }$) and ($^{ }$), respectively. The authors also introduce a distillation procedure, (Duo-DCD), which serves as our primary baseline. Notably, Duo-DCD uses distillation to train a student model that skips some of the teacher's sampling steps. In the next stage, the student becomes the new teacher, progressively shortening the sampling trajectory. Following this idea, we treat Duo-DCD as the teacher in our experiments and apply the same IDLM distillation loss as for the original Duo model, with Duo-DCD as the teacher. Therefore, we apply our method to distill both the original Duo model and the Duo-DCD variant, denoted as and , respectively. Results are summarized in Figure~ . Under Ancestral sampling, IDLM-Duo performs similarly to Duo-DCD at low sampling steps, achieving a $64 $ reduction in steps (from $1024$ to $16$) while preserving GenPPL and output diversity.\n Furthermore, distilling the Duo-DCD model using our IDLM-DCD yields $64 $ speed-up over Duo-DCD (from $1024$ to $16$ steps), and a $128 $ reduction when measured relative to the original Duo model (from $1024$ to $8$ steps), all while preserving performance metrics. \n Under Greedy-Tail sampling, IDLM-Duo shows higher entropy but slightly worse GenPPL than Duo-DCD at low steps. Nevertheless, it still achieves a $64 $ reduction (to $16$ steps), while Duo-DCD gives a $128 $ speed-up (to $8$ steps).\n Further distillation of the Duo-DCD model using our IDLM-DCD yields $32 $ reduction (to $32$ steps) over Duo-DCD, resulting in a $256 $ acceleration over the original Duo model, reducing inference from $1024$ to just $4$ steps, while maintaining comparable metrics.\n\n% \n% TODO",
  "appendix/proofs.tex": "Recall that\n\\[\n =\\{x \\{0,1\\}^N: _{i=1}^N x_i=1\\}\n\\]\nis the set of one-hot tokens, and let \\(D([0,T], )\\) be the Skorokhod space of\ncadlag paths with values in \\( \\). For a path measure \\( Q\\), write\n\\( Q_t\\) for its marginal at time \\(t\\).\n\nWe assume that the teacher exactly minimizes the expected loss and therefore\ninduces the true reverse path measure, \\( ^{f^*}= ^*\\).\nWe also assume that the minimization over \\(f\\) is performed over a sufficiently\nrich model class, so the exact reverse predictor is available for every clean\ndistribution considered. We assume that the student path does not use\ntransitions that are impossible under the teacher path. If\n\\( ^ ^*\\), then\n\\( _{ }( ^ \\| ^*)=+ \\), so the main theorem becomes\nimmediate.\n\nFor a clean distribution \\(p\\), let \\( ^p\\) denote the path measure associated\nwith the fixed forward noising process, written in reverse-time coordinates so\nthat the clean sample is the terminal marginal of \\( ^p\\). Its forward-time\nnoised marginal at noise level \\(t\\) is\n\\[\n p_t(x_t)= q_t(x_t x_0)\\,p(dx_0).\n\\]\nWe write \\( ^ = ^{p_ }\\) and \\( ^*= ^{p^*}\\). Since time\nreversal is a deterministic reparameterization of paths, the KL divergence is\nunchanged by choosing either coordinate convention.\n\nDLM training can be viewed as fitting the reverse-time path measure associated\nwith a fixed forward noising process. Following the main text, we write this\nusing expected losses of the form \\( (f,p)\\), where \\(p\\) is a clean-sample\ndistribution. For SEDD, Dynkin's formula~ gives the\nfollowing distribution-level objective, up to terms independent of the optimized\nmodel:\n\n _{ }(f,p)\n =\n _{p(x_0),t,q_t}\n _{y x_t}\n x_t,y _{Q_t}\n (\n f(x_t,t),y \n -\n s(x_t,t x_0),y \n f(x_t,t),y \n ).\n\nThis is the SEDD instance of the unified loss \\( (f,p)\\) used in\nEquation~ . For a generic clean distribution \\(p\\), define\n\\[\n f^p= _f _{ }(f,p).\n\\]\nAt this minimizer,\n\n f^p(x_t,t),y \n =\n _{p(x_0 x_t)}\n s(x_t,t x_0),y .\n\nIn particular, \\(f^*=f^{p^*}\\) denotes the exact teacher and\n\\(f^ =f^{p_ }\\) denotes the exact fake model for the student\ndistribution.\n\nWe prove the theorem in three steps. First, we show that the path KL equals the\nSEDD inverse-distillation gap. Second, we transfer the result to MDLM and the\nuniform objective, with Duo covered in the hard-token limit. Third, we use the\nchain rule to show that the path KL controls the clean-data KL.\n\n [SEDD path KL equals the inverse gap]\n\nUnder the population assumptions above,\n\n _{ }( ^ \\| ^*)\n =\n _{ }(f^*,p_ )\n -\n _f _{ }(f,p_ ).\n\nThe relative entropy between two continuous-time jump-process path measures can\nbe written as an integral over the relative jump-rate divergence. In the SEDD\nparameterization this gives\n\n _{ }( ^ \\| ^*)\n &=\n _0^T\n _{p_t^ (x_t)}\n _{y x_t}\n x_t,y _{Q_t}\n f^*(x_t,t),y \n\n (\n\n { f^*(x_t,t),y }\n )dt,\n\nwhere \\( (r)=r r-r+1\\). Following the linearization trick\nof~ , for the positive ratios used below,\n\\[\n (r)= _l \\{rl+1- (l)\\}.\n\\]\nUsing the parameterization\n\\[\n l(y,x_t,t)\n =\n\n { f^*(x_t,t),y },\n\\]\nand taking the maximum over admissible \\(f\\), Equation~ \nbecomes\n\n _{ }( ^ \\| ^*)\n =\n _f\n _0^T\n _{p_t^ (x_t)}\n _{y x_t}\n x_t,y _{Q_t}\n dt.\n\nWe now rewrite the expectation in Equation~ . First,\napply the posterior identity~ with\n\\(p=p_ \\):\n\\[\n f^ (x_t,t),y \n =\n _{p_ (x_0 x_t)}\n s(x_t,t x_0),y .\n\\]\nAfter substituting this identity, the only term depending on \\(x_0\\) is\n\\( s(x_t,t x_0),y \\). The remaining term\n\\( f^*(x_t,t)-f(x_t,t),y \\) depends only on \\(x_t\\), \\(t\\), and\n\\(y\\), so it can be placed inside the same conditional expectation:\n\\[\n\n& _{p_t^ (x_t)}\n _{y x_t}\n x_t,y _{Q_t}\n\n\\\\\n&=\n _{p_t^ (x_t)}\n _{p_ (x_0 x_t)}\n _{y x_t}\n x_t,y _{Q_t}\n .\n\n\\]\nFinally, the joint law of \\((x_0,x_t)\\) can be written in two equivalent ways:\n\\[\n p_t^ (x_t)p_ (x_0 x_t)\n =\n p_ (x_0)q_t(x_t x_0),\n\\]\nso\n\\[\n _{p_t^ (x_t)}\n _{p_ (x_0 x_t)}[ ]\n =\n _{p_ (x_0),\\,x_t q_t( x_0)}[ ].\n\\]\nThe maximization over \\(f\\) stays outside this change of variables. Therefore\nwe obtain\n\n _{ }( ^ \\| ^*)\n =\n _f\n _{p_ (x_0),t,q_t}\n _{y x_t}\n x_t,y _{Q_t}\n .\n\nThe right-hand side is exactly the population SEDD loss difference:\n\\[\n _f\n \\{\n _{ }(f^*,p_ )\n -\n _{ }(f,p_ )\n \\}.\n\\]\nBecause the first term is independent of \\(f\\), this equals\n\\[\n _{ }(f^*,p_ )\n -\n _f _{ }(f,p_ ).\n\\]\nThis proves the claim. Importantly, the maximum remains outside the population\nexpectation throughout the argument.\n\n [Gap preservation under equivalent losses]\n\nSuppose two losses satisfy\n\\[\n _A(Tf,p)= _B(f,p)+C(p),\n\\]\nwhere \\(C(p)\\) is independent of the optimized model \\(f\\), and \\(T\\) maps the\ncandidate class for \\( _B\\) onto the corresponding candidate class for\n\\( _A\\). Then their inverse gaps agree:\n\\[\n _A(Tf^*,p)- _g _A(g,p)\n =\n _B(f^*,p)- _f _B(f,p).\n\\]\n\nThe claim follows by cancellation of the model-independent constant:\n\\[\n\n _A(Tf^*,p)- _g _A(g,p)\n &=\n _B(f^*,p)+C(p)\n -\n _f\\{ _B(f,p)+C(p)\\}\n \\\\\n &=\n _B(f^*,p)- _f _B(f,p).\n\n\\]\n\nFor the absorbing diffusion matrix \\(Q_{ }\\) in\nEquation~ , the SEDD objective under the\nclean-token parameterization is equivalent to the MDLM loss\n\\( _{ }\\) up to terms independent of the optimized predictor.\nLemma~ therefore gives\n\\[\n _{ }( ^ \\| ^*)\n =\n _{ }(f^*,p_ )\n -\n _f _{ }(f,p_ ).\n\\]\nFor the uniform diffusion matrix \\(Q_{ }\\) in\nEquation~ , the same argument transfers the\nSEDD gap to the hard-token UDLM objective. In distribution-level notation,\n\\[\n _{ }(f,p)\n =\n _{p(x_0),t,x_t q_t( x_0)}\n .\n\\]\nThus\n\\[\n _{ }( ^ \\| ^*)\n =\n _{ }(f^*,p_ )\n -\n _f _{ }(f,p_ ).\n\\]\nDuo evaluates the same uniform-process objective on a Gaussian-relaxed input and\nrecovers the hard-token UDLM objective as \\( 0^+\\). Hence the theorem\napplies to Duo in this hard-token limit, as stated in the main text. This\nargument does not claim exact uniqueness for finite-temperature Duo.\n\n [Unique solution]\n\nFor SEDD, MDLM, and hard-token UDLM, and for Duo in the limit\n\\( 0^+\\), the IDLM loss satisfies\n\\[\n _{ }( )\n =\n _{ }( ^ \\| ^*)\n\n _{ }(p_ \\|p^*)\n 0.\n\\]\nMoreover,\n\\[\n _{ }( )=0\n\n p_ =p^*.\n\\]\n\nDisintegrate the path measures with respect to their clean-sample marginal:\n\\[\n ^ (dx,dw)=p_ (dx) _x^ (dw),\n\n ^*(dx,dw)=p^*(dx) _x^*(dw).\n\\]\nThe chain rule for relative entropy gives\n\\[\n _{ }( ^ \\| ^*)\n =\n _{ }(p_ \\|p^*)\n +\n _{x p_ }\n _{ }( _x^ \\| _x^*).\n\\]\nThe conditional KL term is nonnegative, so\n\\[\n _{ }( ^ \\| ^*)\n\n _{ }(p_ \\|p^*)\n 0.\n\\]\nNow consider the zero-loss condition. From the previous part of the proof,\n\\( _{ }( )= _{ }( ^ \\| ^*)\\). Therefore,\nif \\( _{ }( )=0\\), then\n\\[\n _{ }( ^ \\| ^*)=0.\n\\]\nSubstituting this into the chain-rule decomposition gives\n\\[\n 0\n =\n _{ }(p_ \\|p^*)\n +\n _{x p_ }\n _{ }( _x^ \\| _x^*).\n\\]\nBoth terms on the right are nonnegative, so each term must be zero. In\nparticular,\n\\[\n _{ }(p_ \\|p^*)=0.\n\\]\nThe KL divergence between two probability distributions is zero only when the\ntwo distributions are equal, hence \\(p_ =p^*\\).\n\nConversely, assume \\(p_ =p^*\\). The forward noising process is fixed: once\nthe clean distribution \\(p\\) is chosen, the whole path measure \\( ^p\\) is\nobtained by applying the same noising dynamics to samples from \\(p\\). Therefore,\nif the clean distributions are equal, the induced path measures are also equal:\n\\[\n ^ = ^{p_ }= ^{p^*}= ^*.\n\\]\nThus\n\\[\n _{ }( ^ \\| ^*)=0,\n\\]\nand using again\n\\( _{ }( )= _{ }( ^ \\| ^*)\\), we get\n\\( _{ }( )=0\\).\n\nThe proof above is for one token. For a full sequence, the same proof works in\nan ideal setting where the whole sequence is treated as one state and the models\ndescribe the full reverse process over sequences. In that case, the IDLM loss is\nthe KL divergence between the full sequence path measures, so it also controls\nthe difference between \\(p_ (x_0^{1:L})\\) and \\(p^*(x_0^{1:L})\\). This is\nnot exactly the loss used in experiments. In practice, we sum token-level losses,\nwhich is convenient for training but does not by itself give the full sequence\npath KL, because tokens may still depend on each other. Thus, the proof covers\nthe one-token objective and the ideal full-sequence objective, but not the\npractical factorized sequence loss. We leave a formal theoretical analysis of\nthe factorized sequence-level IDLM objective as a promising direction for future\nresearch.",
  "appendix/algorithm.tex": "The pseudocode for the proposed Inverse-distilled Diffusion Language Models (IDLM) training procedure is presented in Algorithm~ . The method follows an alternating optimization scheme in which the student generator $G_{ }$ and the auxiliary fake diffusion model $ $ are updated iteratively. Both models are initialized from a pretrained teacher diffusion model $f^*$. At each training step, we first update the fake model using samples generated by the current student model and the teacher loss objective. Subsequently, the student generator is updated using the IDLM loss, which measures the discrepancy between the teacher and fake model predictions on student-generated inputs. A helper function is defined to sample training $(x_t, t)$ from the data distribution and the associated conditional forward process. A visual overview of the entire method is provided in Figure~ .\n\n [t]\n\n {Input} {Output}\n {Teacher network $f^{*}$; \\\\\n Teacher Loss $ $; {Section~ }\\\\\n Samples from target distribution $p^*( _0)$; \\\\\n Number of student iterations $K$;\n }\n\n {Learned generator $G_{ }$}\n\n $G_ (f^*)$\n\n $ (f^*)$\n\n {func}{}{} \n {$ (p^*):$}{\n $ _0 p^*( _0)$ \\\\\n $t $ \\\\\n $x_{ } p_{ 0}(x_{ } _0)$ {Equation~ , in case of the Duo we use $x_{ }^{ }(w_{ })$ ( )}\n $x_{ }, $ \n }{}\n\n {\n\n $x_{t'}, t' (p^*)$ \\\\\n $ _{ } ( , G_{ }(x_{t'}, t'))$ {Equation~ }\n Update $ $ by using $ { _{ }}{ }$\n\n $x_{ }, (p^*)$ \\\\\n $ _{ }( ) $ {Equation~ }\n Update $G_{ }$ by using $ { _{ }( )}{ }$\n }",
  "appendix/additional_experimental_results.tex": "Table~ reports the detailed Duo$^{ }$ results corresponding to the main OWT comparison. As mentioned above, Figure~ shows the corresponding GenPPL and entropy curves together with the other step-scaling results. Under ancestral sampling, IDLM-DCD remains strongest in the aggressive low-step regime. In particular, the 8-step sampler gives the clearest speed-quality tradeoff: it stays close to the 1024-step Duo teacher while reducing the reverse-step budget by $128 $.\n\n [t]\n { {Duo$^{ }$ distillation comparison.} We report GenPPL $ $, MAUVE $ $, and Entropy $ $. Methods are grouped by Steps, with the best metrics bolded in each group.}\n\n {5pt}\n {lcccc}\n\n & { Steps} & { GenPPL $ $} & { MAUVE $ $} & { Entropy $ $} \\\\\n\n FLM~ & {*}{1024} & & -- & 5.33 \\\\\n Duo$^{ }$ (Teacher)~ & & 77.69 & $ $ 0.01 & \\\\\n\n ELF~ & {*}{32} & $ $ 0.16 & -- & 5.14 \\\\\n FMLM~ & & 45.09 & -- & 5.25 \\\\\n Duo-DCD$^{ }$~ & & 61.31 & $ $ 0.01 & \\\\\n IDLM-DCD$^{ }$ ( ) & & 42.13 $ $ 3.65 & 0.94 $ $ 0.01 & 5.41 $ $ 0.05 \\\\\n\n ELF~ & {*}{16} & $ $ 1.09 & -- & 5.16 \\\\\n FMLM~ & & 63.63 & -- & 5.29 \\\\\n Duo-DCD$^{ }$~ & & 75.24 & $ $ 0.01 & \\\\\n IDLM-DCD$^{ }$ ( ) & & 52.06 $ $ 4.23 & 0.95 $ $ 0.01 & 5.44 $ $ 0.05 \\\\\n\n ELF~ & {*}{8} & 67.32 $ $ 2.25 & -- & 5.14 \\\\\n FMLM~ & & 86.5 & -- & 5.36 \\\\\n Duo-DCD$^{ }$~ & & 111.88 & 0.94 $ $ 0.01 & \\\\\n IDLM-DCD$^{ }$ ( ) & & $ $ 4.59 & $ $ 0.01 & 5.42 $ $ 0.03 \\\\\n\n FMLM~ & {*}{4} & 111.31 & -- & 5.26 \\\\\n Duo-DCD$^{ }$~ & & 261.82 & 0.79 $ $ 0.02 & \\\\\n IDLM-DCD$^{ }$ ( ) & & $ $ 8.39 & $ $ 0.02 & 5.32 $ $ 0.06 \\\\\n\nThe main comparison is with Duo-DCD under the same ancestral sampler. At 8 steps, IDLM-DCD improves GenPPL from 111.88 to 66.41 while keeping MAUVE and entropy close to the teacher. At 4 steps, Duo-DCD degrades sharply, whereas IDLM-DCD still preserves a usable quality-diversity tradeoff. Thus the benefit of IDLM is largest when very few reverse steps must replace the original 1024-step teacher trajectory.\n\n { {GenPPL and Entropy vs. Sampling Steps}}\n\nFigure~ provides the main GenPPL and entropy curves that complement the compact OWT summary in Figure~ . These plots show how generation quality and token-level diversity change as the number of reverse sampling steps is reduced. For MDLM, IDLM-MDLM gives the strongest low-step tradeoff relative to the teacher and SDTT baselines, improving GenPPL while maintaining comparable entropy. For Duo$^{ }$, IDLM-DCD is most effective in the aggressive low-step regime, reaching strong GenPPL at $4$ steps while preserving competitive entropy. We also include the ancestral Duo curve here, so the greedy and ancestral step trends can be compared in one place.\n\n [t]\n\n [t]{0.32 }\n\n [t]{0.32 }\n\n {Duo$^{ }$}\n\n [t]{0.32 }\n\n {Duo$^{ }$}\n\n { Each panel shows how generation quality and diversity change as the reverse sampling budget is reduced. GenPPL measures sample quality, where lower is better, while entropy tracks output diversity, where higher is better. The panels provide a unified comparison of step-scaling behavior for our IDLM-MDLM, IDLM-DCD$^{ }$, and IDLM-DCD$^{ }$ models.}\n\nTo test whether IDLM's low-step behavior persists beyond the $169$M setting, we distill an $863$M MDLM~ checkpoint from the SDTT~ work. This is the most direct larger-scale comparison available because SDTT uses the same model family and provides matched low-step baselines. As shown in Table~ , IDLM-MDLM consistently improves over SDTT at $32$, $16$, and $8$ sampling steps, improving both GenPPL and entropy in the low-step regime. In particular, the $16$-step sampler preserves the $64 $ reduction from the $1024$-step teacher, indicating that the acceleration behavior observed in the $169$M experiments also extends to the larger-scale regime.\n\n [t]\n\n { We report GenPPL $ $ and Entropy $ $.}\n\n {lccc}\n\nMethod ($863$M params) & Steps & GenPPL $ $ & Entropy $ $ \\\\\n\nMDLM (Teacher)~ & 1024 & 34.99 & 5.24 \\\\\n\nSDTT~ & {*}{32} & 31.16 & 5.12 \\\\\nIDLM-MDLM & & $ $ 1.70 & $ $ 0.10 \\\\\n\nSDTT~ & {*}{16} & 52.13 & 5.20 \\\\\nIDLM-MDLM & & $ $ 2.13 & $ $ 0.04 \\\\\n\nSDTT~ & {*}{8} & 121.51 & 5.25 \\\\\nIDLM-MDLM & & $ $ 4.11 & $ $ 0.03 \\\\\n\nThis subsection expands the ablations summarized in\nSection~ . We keep the teacher and evaluation setup fixed, so\nthe comparisons isolate how gradients are passed to the generator and how much\nlatent stochasticity is available during masked-diffusion sampling.\n\n {Beyond the parameterization.}\nIn masked diffusion, the parameterization makes the IDLM-MDLM\ngenerator update mask-only: unmasked positions are copied by both the teacher\nand fake model, so their contribution cancels. As discussed in\nSection~ , this removes the sampled\nintermediate token from the generator gradient and behaves like an implicit\nstop-gradient through the absorbing transition. We therefore test whether\nexplicitly differentiating through the intermediate state improves the update.\nLet $x_0=G_ ( )$ and let $m$ denote the mask token. The Gumbel\nrelaxation softens the full categorical draw from $q_t( x_0)$,\nreplacing the hard token by\n\\[\n ^{ }_t\n=\n \\! (\n { }\n ),\n\ng_i { }{ } (0,1).\n\\]\nIts objective is\n\\[\n _{ }( )\n=\n- _{t, ,g}\n\nG_ ( ),\n f^*( ^{ }_t,t)\n-\n f( ^{ }_t,t)\n .\n\\]\nThe Argmax relaxation keeps the forward state closer to the hard masked\nprocess by using the Gumbel-max form of\n$q_t(x_t x_0)= _t _{x_0}(x_t)+(1- _t) _m(x_t)$:\n\\[\nb_{ }\n=\n \\{ _t + g_1\n\n (1- _t)+g_2\\},\n\nb_{ }=1-b_{ },\n\\]\n\\[\n ^{ }_t\n=\nb_{ }G_ ( )\n+\nb_{ }m,\n\ng_1,g_2 { }{ } (0,1).\n\\]\nThe corresponding objective is\n\\[\n _{ }( )\n=\n- _{t, ,g_1,g_2}\n\nG_ ( ),\n f^*( ^{ }_t,t)\n-\n f( ^{ }_t,t)\n .\n\\]\nFigure~ shows that this additional gradient path does\nnot improve the masked-diffusion tradeoff. The update reaches the\nuseful low-GenPPL regime fastest while preserving substantially more entropy\nthan Argmax. Gumbel is smoother, but it reduces GenPPL more slowly and gives a\nweaker quality--diversity profile near the shared training budget. This\nsupports the main-text hypothesis that feeding soft or branch-switched\nintermediate states to an MDLM teacher can move the evaluation outside the\none-hot training domain and weaken the distillation signal.\n\n { Training steps (k)}\n\n {\n\n Validation GenPPL and entropy for IDLM-MDLM with the ,\n Gumbel, and Argmax generator updates. The update gives the\n strongest early quality--diversity tradeoff under the shared training\n budget.\n }\n\nFor uniform diffusion, the Duo parameterization evaluates denoisers on relaxed\nstates, so the intermediate state remains differentiable. This makes the\ndifference between full inverse distillation and a DMD-like stop-gradient\nreduction directly testable. Let\n\\[\n\na_t(w,x)\n&= g_{ } (x_t(w),x,f^*(x_t^ (w),t) )\\\\\n& - g_{ } (x_t(w),x,f(x_t^ (w),t) ),\n\n\\]\nwhere \\(x_t(w)= (w)\\) and \\(x_t^ (w)= (w/ )\\) are the\nhard and relaxed Duo states. The full IDLM-Duo objective and the DMD-like\nstop-gradient objective use the same teacher--fake Duo advantage, but differ in\nwhether the relaxed intermediate state carries gradients:\n\nw_t\n&= _t G_ ( )\n + {1- _t^2}\\, ,\\\\\nw_t^{ }\n&= _t \\! (G_ ( ) )\n + {1- _t^2}\\, ,\\\\\n _{ }^{ }( )\n&= \\! ,\\\\\n _{ {DMD like}}^{ }( )\n&= \\! .\n\nFigure~ shows that this distinction matters in the\nuniform case. The stop-gradient variant quickly enters a high-GenPPL,\nhigh-entropy regime, so its entropy reflects instability rather than useful\ndiversity. In contrast, the full IDLM-Duo update remains controlled for most of\ntraining. Thus, the formal connection between IDLM and DMD-like matching does\nnot imply that the two generator updates have the same optimization behavior\nfor uniform diffusion; the pathwise gradient through the relaxed intermediate\nstate is empirically important.\n\n {\n\n Validation GenPPL and entropy for IDLM-Duo and its DMD-like\n stop-gradient variant. Removing the intermediate-state gradient leads to a\n divergent high-GenPPL regime, whereas the full IDLM update remains stable.\n }\n\nFinally, we test whether extra latent randomness helps masked diffusion in the\nextreme low-step regime. Unlike uniform diffusion, masked diffusion starts from\na deterministic all-mask terminal state, so a deterministic one-step generator\nhas no rich latent source for sample diversity. We therefore inject Gaussian\nnoise into the IDLM-MDLM input as a simple control. Table~ \nshows that this modification does not improve GenPPL or entropy:\n\n {\n\n Values are GenPPL with entropy in parentheses.\n }\n\n {c|cc}\nSteps & IDLM-MDLM & IDLM-MDLM + Gaussian noise \\\\\n\n1 & 4056.12 (5.87) & 4096.54 (5.88) \\\\\n8 & 79.75 (5.61) & 81.61 (5.61) \\\\\n16 & 32.75 (5.42) & 35.76 (5.45) \\\\\n32 & 20.45 (5.23) & 21.03 (5.26)\n\nThe negative result is consistent with Section~ : simple\ninput-level Gaussian noise is insufficient for masked diffusion, and improving\nthe low-step regime may require stochasticity that is matched more closely to\nthe discrete absorbing process.",
  "appendix/experimental_details.tex": ". Our implementation builds upon the original Duo~ {repository}~ and the SEDD~ {repository}~ , which serve as the primary codebases for our experiments. We extend these repositories to incorporate our proposed training framework, {with the main algorithmic details provided in Algorithm~ .}\n\n . We use pretrained checkpoints for the distillation of MDLM and Duo models obtained from the Duo~ {repository}~ , and a pretrained checkpoint for SEDD distillation from the SEDD~ {repository}~ . Unfortunately, for SEDD, only a checkpoint corresponding to the absorbing process was available.\n\n . We follow~ for distilling MDLM and Duo, and design the denoising model to operate on both continuous and discrete latents. We use a Transformer~ in which the first-layer token representations are computed via a matrix multiplication with the embedding matrix. For discrete inputs, we perform standard embedding lookups. In contrast, continuous inputs correspond to \u201csoft lookups,\u201d producing a convex combination of vocabulary embeddings. For SEDD distillation, we follow~ and use a model that only accepts one-hot vectors. For all setups we use small models with $169$M parameters, specifically with $12$ layers, $12$ heads, hidden size $768$, sequence length $1024$, and dropout $0.1$.\n\n . \nAcross all experimental settings, we employed a per-device batch size of 8, resulting in a global batch size of 512. Optimization was performed using the AdamW optimizer~ , with a fixed learning rate of $1 10^{-6}$ for MDLM and Duo, and $3 10^{-7}$ for SEDD. A constant learning rate schedule was used with 2500 warmup steps for all configurations. We adopted the log-linear noise formulation from~ in the loss function, and applied exponential moving average (EMA) with a decay rate of 0.9999 throughout training.\n\n . \nAs observed by~ , the GenPPL metric is sensitive to floating-point precision. To ensure consistency and numerical stability, we follow the protocol of~ and perform all sampling experiments using precision. All evaluation metrics are computed using the official codebase provided in the Duo~ repository.\n\n .\nLet \\(x_0^{1:L} p_ \\) denote generated samples and let \\(p^*\\) denote the validation-data distribution. For a finite generated set \\( _ \\), generative perplexity (GenPPL) is computed by scoring each generated sequence with a fixed GPT-2 Large autoregressive evaluator \\(p_{ }\\)~ :\n\\[\n\n=\n \\! (\n- {M}\n _{x_0^{1:L} _ }\n _{i=1}^{L}\n p_{ }(x_0^i x_0^{1:i-1})\n ),\n\\]\nwhere \\(M\\) is the total number of evaluated tokens. Lower GenPPL means that the evaluator assigns higher likelihood to generated samples, and we report it together with diversity metrics to avoid favoring overly repetitive text. Entropy is the empirical token entropy of generated samples:\n\\[\n\n=\n- _{v } _ (v) _ (v),\n\\]\nwhere \\( _ (v)\\) is the generated-token frequency of token \\(v \\); higher entropy means greater token-level diversity. MAUVE~ compares samples from \\(p_ \\) with validation samples from \\(p^*\\) in language-model embedding space, higher values mean that the two distributions are closer. Finally, GM denotes the GPT-2 Gradient Moment metric~ :\n\\[\n (p_ ,p^*)\n=\n \\|\n _{x_0^{1:L} p_ } _{ } p_{ , }(x_0^{1:L})\n-\n _{x_0^{1:L} p^*} _{ } p_{ , }(x_0^{1:L})\n \\|_2^2,\n\\]\nestimated with independent minibatches. Lower GM means that generated samples induce reference-model gradients closer to those induced by validation data.\n\n .\nFollowing prior work~ , we preprocess the One Billion Words dataset using the detokenization procedure described by~ and~ , with the official implementation available at {this link}. For the OpenWebText dataset, we utilize the tokenizer and apply a similar concatenation and wrapping procedure as in the previous works~ , targeting a sequence length of 1,024 tokens. During wrapping, tokens are inserted between consecutive sequences. As OpenWebText does not include an official validation split, we reserve the final 100{,}000 documents from the dataset as a validation set.\n\n .\nOur primary baselines are SDTT~ and Duo-DCD~ . Performance metrics for both methods are reported using values provided in the Duo~ , corresponding to the best-performing results obtained after 5 rounds of distillation, described in~ . While a valid pretrained checkpoint was available for Duo-DCD, we were unable to identify a usable checkpoint for SDTT~ , which precluded a fair comparison or distillation of this model within our experimental framework.\n\n .\nWe use TinyGSM for training and validation and tokenize examples with the tokenizer. Following the conditional-generation setup, examples are kept at sequence length $512$ without document wrapping. We insert tokens, use a newline separator between the problem and solution fields, filter examples that exceed the maximum length, and reserve $1\\%$ of the data for validation with seed $42$. The problem prompt is treated as conditioning context and is excluded from the training loss, while padding tokens are included.\n\n .\nWe use the same small denoising Transformer architecture as in the OpenWebText experiments: $12$ layers, $12$ attention heads, hidden size $768$, sequence length $512$, and dropout $0.1$. IDLM-MDLM uses the MDLM parameterization with log-linear noise and is initialized from the TinyGSM MDLM teacher checkpoint. IDLM-Duo uses uniform diffusion with the mean parameterization, time conditioning, and adaptive layer normalization; the student is initialized from the TinyGSM Duo teacher, using the teacher EMA weights.\n\n .\nBoth IDLM-MDLM and IDLM-Duo are trained with adversarial distillation enabled at every update. We use a global batch size of $512$, $4$ GPUs, DDP training, and AdamW with learning rate $1 10^{-6}$, weight decay $0$, $ _1=0.9$, $ _2=0.999$, and $ =10^{-8}$. IDLM-MDLM uses per-device batch size $32$ and bfloat16 precision, while IDLM-Duo uses per-device batch size $16$ and float32 precision. We train for $250{,}000$ steps with a constant learning-rate schedule after $2{,}500$ warmup steps, use antithetic time sampling, and maintain an EMA copy of the model with decay $0.9999$.\n\n .\nWe follow the conditional-generation protocol of~ . Each GSM8K evaluation instance is formatted as a problem prompt followed by a solution region. During sampling, the problem prompt is clamped and the model generates only the solution region. We evaluate IDLM-MDLM and IDLM-Duo with $32$, $64$, and $128$ reverse steps and compare them against the corresponding $1024$-step teachers. IDLM-MDLM uses the cached ancestral predictor, IDLM-Duo uses the ancestral sampler, and both use precision at sampling time.\n\n .\nFor each problem, we generate one completion and use the execution-based TinyGSM/GSM8K scorer to extract the predicted numerical answer. We report exact-match accuracy against the GSM8K ground-truth answer in Table~ .",
  "arxiv/sec/abstract.tex": "Diffusion Language Models (DLMs) have recently achieved strong results in text generation. However, their multi-step sampling leads to slow inference, limiting practical use. To address this, we extend Inverse Distillation, a technique originally developed to accelerate continuous diffusion models, to the discrete setting.\n Nonetheless, this extension introduces both theoretical and practical challenges. From a theoretical perspective, the inverse distillation objective lacks uniqueness guarantees, which may lead to suboptimal solutions. From a practical standpoint, backpropagation in the discrete space is non-trivial and often unstable. To overcome these challenges, we first provide a theoretical result demonstrating that our inverse formulation admits a unique solution, thereby ensuring valid optimization. We then introduce gradient-stable relaxations to support effective training. \n As a result, experiments on multiple DLMs show that our method, , reduces the number of inference steps by $4 $-$64 $, while preserving the teacher model\u2019s generation quality. We provide the code, model checkpoints, and video tutorials on the project page: \n =-1\n\n { {https://david-cripto.com/idlm}}",
  "arxiv/sec/introduction.tex": "[t!]\n\n { DLMs learn a slow teacher $f^*$ from data. IDLM fixes $f^*$ and learns $p_ $ for fast few-step generation.}\n\n Generative modeling for discrete data, such as natural language, has seen widespread adoption, driven by the rapid advancement of large language models~ . \n Recently, Diffusion Language Models \n (DLMs; ) have emerged as a promising framework for modeling the distribution of the text data. A general formulation for a broad class of DLMs was introduced by~ . However, the general formulation is often intractable in practice and introduces {additional} theoretical complexity. To address this, subsequent work~ has focused on specific types of diffusion processes, enabling simplified theoretical analyses and more tractable implementations. As a result, DLMs have achieved competitive performance in text generation, comparable to that of autoregressive language models~ . However, the inherently iterative nature of reverse diffusion requires hundreds or even thousands of sampling steps at inference time, resulting in considerable latency that limits their practicality.\n\n On the other hand, in the continuous domain, the issue of slow inference in diffusion models~ has been the focus of extensive research~ . Among the various approaches, has emerged as one of the most effective solutions~ . However, directly applying such techniques in the discrete domain is challenging due to the non-differentiable nature of discrete data. Recent efforts have begun to address this limitation for DLMs. Notably, SDTT~ and Duo-DCD~ propose adaptations of consistency-based distillation methods~ to the discrete setting, demonstrating initial progress toward enabling fast inference in DLMs.\n\n In this work, we investigate an alternative distillation paradigm by extending the theoretical framework of ~ , originally developed for continuous diffusion models, to the discrete domain. However, this direct extension introduces several nontrivial challenges arising from the intrinsic properties of discrete data. First, emerge regarding the validity of the training objective, particularly due to the potential non-uniqueness of its minimizers. Second, arise from the non-smoothness of the discrete space, which complicates the optimization. \n\n To address the above-mentioned issues, we present , a principled framework for accelerating a broad class of discrete diffusion models. In summary, our core contributions are as follows:\n\n {1pt}\n {1pt}\n We theoretically establish the uniqueness of the global optimum for the IDLM objective, validating the correctness of the proposed optimization procedure ( ).\n We propose gradient-stable relaxations for effective training {in the non-smooth discrete domain} ( ).\n We empirically demonstrate that IDLM achieves competitive generation quality, matching the performance of 1024-step teacher models while requiring up to $64 $ fewer sampling steps ( ).",
  "arxiv/sec/method.tex": "In this section, we present our core methodology for distilling a broad class of Diffusion Language Models into a few-step generator. We begin by providing the theoretical foundations supporting the extension of the Inverse Distillation framework to the discrete domain ( ). We then address the practical challenges arising from the non-smooth nature of discrete spaces ( ). Lastly, we outline key implementation details {and introduce the sequence-level scaling of the proposed objective} ( ). Appendices~ and~ give the masked- and uniform-diffusion formulations used below.\n\n To address the issue of slow inference, we extend the Inverse Distillation framework, originally developed for continuous diffusion models, to the domain of Diffusion Language Models. Then, following the logic of the continuous case, described in ( ), we propose to consider the following objective for the discrete diffusion:\n\n _{ } ( ) = (f^*, p_{ }) \\!\n - \\! _{f} (f, p_{ }).\\!\n\n However, simply formulating this objective in a manner analogous to the distillation loss used in continuous diffusion models does not guarantee the validity of its solutions. The absence of a uniqueness guarantee implies that the optimization procedure may converge to suboptimal solutions. To address this, we present the following theorem. \n\n [Unique solution]\n\n For the SEDD~ , MDLM~ , and Duo~ (in the limit as $ 0^{+}$) objectives the IDLM loss defined in equation~ satisfies\n\n _{ }( ) _{ }(p_{ } p^*) 0 \n\n and achieves its minimum (zero) if and only if the model distribution matches the target distribution\n\n _{ }( ) = 0 p_{ } = p^*. \n\n We give the proof and discuss the sequence-level case in Appendix~ .\n\n The IDLM loss has a simple probabilistic interpretation: it matches full diffusion trajectories. If $ ^ $ and $ ^*$ denote the trajectory distributions induced by $p_ $ and $p^*$, respectively, then the proof shows\n \\[\n _{ }( )\n =\n _{ }( ^ \\| ^*).\n \\]\n Because the final clean sample is part of the trajectory, this KL controls $ _{ }(p_ \\|p^*)$, which is exactly the inequality in Theorem~ . Hence zero IDLM loss can occur only when the student recovers the data distribution.\n\n% \n% DMD-style continuous distillation~ matches noised marginals across diffusion time:\n% \\[\n% _{ }( )\n% =\n% _0^1 w(t)\\,\n% _{ }(p_t^ \\|p_t^*)\\,dt .\n% \\]\n% Here $p_t^ $ and $p_t^*$ are the student and target marginal distributions at time $t$. Recent discrete methods such as D-MMD~ and DiDi-Instruct~ follow this marginal-matching view. IDLM is related, but it matches the whole stochastic path rather than only sampled-time marginals. As shown in RSD work~ , a DMD-like update is recovered from inverse distillation after stop-gradient (see Appendix~ ). It is worth to note that in masked diffusion, these losses coincide because the MDLM parameterization implicitly applies this stop-gradient to the intermediate states, while in uniform diffusion this distinction is important.\n\n We now explain the main optimization problems that arise when applying IDLM in a discrete domain. For clarity, we use MDLM as an example because the absorbing process gives the simplest setting in which these bottlenecks can be seen explicitly. The same problems also arise for uniform diffusion. Appendix~ expands the MDLM-specific inverse distillation simplification used below. Appendix~ gives the corresponding uniform-diffusion specialization. Specializing the IDLM objective to the MDLM loss gives\n\n _{ }( )\n =\n _{ }(f^*,p_ )\n -\n _f _{ }(f,p_ ). \n\n Here we use the same MDLM loss as in the Equation~ , replacing the data distribution $p^*$ by the student distribution $p_ $. This is the quantity through which the distribution $p_ $ must be optimized:\n\n _{ }(f,p_ )\n \\!\\! =\\!\\!\n -\n _{p_ (x_0),\\,t,\\,q_t}\n .\n\n In practice, we parametrize ditribution $p_{ }$ by model ${G_{ } }$, which transforms latent variable $ $ to a discrete output $x_0 $. The latent space $ $ is equipped with a tractable prior distribution $p_{ }$, inducing a distribution $p_{ }$ over $ $ via the mapping $G_{ }$:\n \\[\n p_{ E},\n\n x_0=G_{ }( ).\n \\]\n In the equations below, we highlight with {red} quantities whose values depend on the generator parameter {$ $}. Substituting the generator sample into~ gives the loss:\n\n _{ }(f,p_ )\n =\n -\n _{ ,\\,t,\\,\n {x_t} q_t( G_{ { }}( ))}\n . \n\n This formulation reveals two gradient bottlenecks. First, if $G_{ { }}$ is forced to output a one-hot token in $ $, then the generator output is not a smooth function of its logits. Second, $ {x_t}$ is sampled from $q_t( G_{ { }}( ))$, hence the noised token depends on $ { }$ through the probabilities of this categorical distribution. Backpropagating through the objective would therefore require passing gradients through the sampling of $ {x_t}$. This is not handled by automatic differentiation, and common alternatives such as hard Gumbel-Softmax can be unstable.\n\n To remove the first bottleneck, we relax the generator range from $ $ to the simplex $ $:\n \\[\n G_{ { }}( ) .\n \\]\n This relaxation is compatible with both ingredients of the MDLM loss: the cross-entropy term remains a valid soft-label loss, and the absorbing conditional distribution $q_t( ~ ~G_{ { }}( ))$ remains a categorical distribution when $G_{ { }}( ) $.\n Figures~ ,~ , and~ in Appendix~ visualize why this remains a valid update and how it changes a one-hot token loss into a weighted token loss.\n Appendix~ uses the same relaxation for uniform diffusion.\n\n {MDLM: parameterization.}\n For MDLM, the parameterization copies already unmasked tokens: if $x_t m$, then $f^*(x_t,t)=f(x_t,t)=x_t$ for any model. Consequently, the corresponding token contribution to $ _{ }$ is zero. Hence the IDLM update is nonzero only when the noised token is the mask token, $x_t=m$. The absorbing-process details are given in Appendix~ . Appendix~ and Figure~ show this mask-only cancellation explicitly. Therefore, for the generator update, the MDLM loss reduces to\n\n _{ }(f,p_ )\n =\n -\n _{ ,t}\n . \n\n The sampled token $x_t$ no longer appears in the generator update. The remaining dependence on $ { }$ is only through the differentiable simplex-valued output $G_{ { }}( )$. Appendix~ explains why this can be viewed as an implicit stop-gradient through the intermediate state for masked diffusion.\n\n Duo provides a differentiable path by introducing an auxiliary Gaussian variable $ $ and using the relaxed state\n\n {x_t}\n =\n \\! (\n {\n _t G_{ { }}( )\n +\n {1- _t^2}\\, \n }{ }\n ),\n\n (0,I), \n\n where the {red} terms indicate the path through the generator parameters. The map $G_{ { }}( ) {x_t}$ is differentiable. This is possible in the Duo parameterization because the denoiser is trained on simplex-valued states rather than only on discrete one-hot tokens.\n More details are given in Appendix~ .\n\n The IDLM objective contains an inner optimization over the fake diffusion model $f$. In practice, we approximate this nested problem by\n alternating two updates, following prior work~ .\n\n First, we fix the student generator $G_{ }$ and train\n $f$ on samples from the current student distribution\n $p_{ }$. This approximates the inner minimization in the IDLM\n objective:\n\n { } f \n { } \\\\\n _{f} = (f, p_{ }). \n\n Second, we fix $f$ and update $G_{ }$ using the IDLM gap:\n\n { } \n { } f \\\\\n _{ }( )\n =\n (f^*, p_{ })\n -\n (f, p_{ }). \n\n These two updates are summarized visually in Figure~ ,\n which depicts the fake model and the student generator as the two\n learnable components of the IDLM training loop.\n\n For MDLM, the parameterization makes the generator update\n especially transparent: only masked positions contribute. Substituting the\n mask-only MDLM loss into the IDLM gap gives\n \\[\n _{ }( )\n =\n - _{ ,t}\\!\n .\n \\]\n Here \\(a_t= f^*(m,t)- f(m,t)\\), and \\(f\\) is the current fake model. Thus\n each masked position gives the\n generator a teacher-over-fake advantage vector \\(a_t\\). If\n \\(G_ ( )= (z_ )\\), then for a fixed \\(( ,t)\\)\n \\[\n { _{ }}{ z_{ ,i}}\n =\n - _t G_{ ,i}( )\n (a_{t,i}- G_ ( ),a_t ).\n \\]\n Hence gradient descent increases token \\(i\\)'s logit exactly when its advantage\n is above the generator's current average advantage. In words, the student does\n not simply chase the teacher's most likely token, it promotes tokens that the\n teacher prefers more than the fake model does. When the fake model catches up\n to the teacher on student samples, this relative advantage vanishes.\n\n { \n {2.5pt}\n {@{}>{ }p{0.31 }>{ }p{0.23 }>{ }p{0.37 }@{}}\n\n Case & Token behavior & Intuition \\\\\n\n \\(a_{t,i}> G_ ,a_t \\)\n & logit increases\n & $f^*$ prefers token \\(i\\) more than $f$ does \\\\\n \\(a_{t,i}< G_ ,a_t \\)\n & logit decreases\n & token \\(i\\) is below the current advantage \\\\\n \\(a_t 0\\)\n & no relative signal\n & $f$ has matched $f^*$ on student samples \\\\\n\n }\n\n For uniform diffusion, the same idea applies to a noised transition rather\n than only to a masked clean-token prediction. The local advantage becomes\n \\[\n\n a_t\n =\n g_{ }(x_t,G_{ }( ),f^*(x_t,t))-\n g_{ }(x_t,G_{ }( ),f(x_t,t)),\n\n \\]\n Appendix~ gives \\(g_{ }\\), and Appendix~ expands the advantage.\n Since uniform diffusion can revise tokens repeatedly, this advantage guides\n both which token to choose and when the reverse path should jump.\n\n Standard discrete diffusion samplers usually draw the next state\n conditionally independently across output positions, even though each\n factor can attend to the whole current sequence. With many reverse steps,\n the composition of these local transitions can still represent a rich\n sequence distribution. In the few-step regime, however, each reverse step\n must replace many small teacher updates, making this factorization a\n bottleneck.\n IDLM addresses this bottleneck by learning a mixture of sequence-level\n distributions, as described next.\n\n [!t]\n\n [t]{0.49 }\n {IDLM: training.}{One branch is active per update: fit the fake model or distill the student.}\n [language=IDLMPseudocode, escapechar=`, basicstyle= , breaklines=true, gobble=4]\n ` {teacher , fake model , and student generator }`\n ` {mode in \\{ , \\} selects the updated component}`\n\n x_0 = ` `(p_data)\n t = ` `(0, 1)\n x_t = ` `(x_0, t) ` `\n x_hat = ` `(x_t, t)\n\n ` ` mode == ` `:\n loss = ` `(` `, x_hat)\n ` `(` `, loss)\n ` `:\n loss = ` `(` `, x_hat) - ` `(` `, x_hat)\n ` `(` `, loss)\n\n [t]{0.49 }\n {IDLM: few-step sampling.}{The reverse transition is chosen by the diffusion process.}\n [language=IDLMPseudocode, escapechar=`, basicstyle= , breaklines=true, gobble=4]\n ` `\n\n ` `\n x_t = ` `(process)\n ` ` k ` ` ` `(K, 0, -1):\n t = grid[k]\n t_prev = grid[k - 1]\n x_hat = ` `(x_t, t)\n\n ` `\n x_prev = ` `(x_t, x_hat, t, t_prev)\n x_t = x_prev\n ` ` x_t\n\n A full distribution over $ ^L$ would require $N^L$\n probabilities, which is intractable to parameterize directly.\n The key is that the student uses a structured mixture: it samples a\n shared variable $ p_{ E}$ and outputs\n $G_ ^{1:L}( )\n =(G_ ^1( ), ,G_ ^L( )) ^L$.\n For this fixed $ $, the component may factorize over positions,\n \\[\n\n p_ (x_0^{1:L} )\n &=\n _{i=1}^L\n \\! (x_0^i;G_ ^i( ) ),\\\\\n p_ (x_0^{1:L})\n &=\n _{ p_{ E}}\n .\n\n \\]\n The product describes one component, while the expectation mixes many\n such components. Since the same $ $ controls all positions, a\n component can encode a sentence-level choice. Although we use\n simplex-valued outputs for differentiable training, in theory the\n generator should produce vocabulary tokens. Hence, during training, each\n $G_ ^i( )$ should concentrate near a one-hot token, so the\n component degenerates to a point mass on one full sequence. Thus the\n generator represents text as a mixture over coherent sequence-level atoms,\n allowing $p_ $ to capture semantic correlations without explicitly\n enumerating the full sequence space.\n A similar latent-mixture intuition appears in VADD~ \n and Di4C$^2$~ , but with different\n objectives.\n\n% \n% \n% \n% {Having addressed the principal theoretical and practical challenges of the proposed method, we now turn to its empirical aspects. This section provides additional technical details that are essential for implementation of our method. We assume that all techniques introduced in ( ) are applied throughout this section and in the remainder of the paper.}\n\n% \n% Following prior distillation approaches that employ an auxiliary fake model during training~ , we initialize both the fake diffusion model and the student generator using the parameters of the pretrained teacher diffusion model.\n\n% \n% We observe that distilling a single-step generator for text generation remains practically challenging. This observation motivates us to follow prior works~ and extend the IDLM framework to support multistep generation. \n% Specifically, to enable few-step inference, and given that the student generator is initialized from the pretrained teacher model, we set the latent space $ $ to be the same input space as for the teacher . Therefore, the generator is parameterized as $G_{ } [0, 1] $ in the SEDD and MDLM settings, and as $G_{ } [0, 1] $ in the Duo setting. To avoid notational ambiguity, we denote samples from the target data distribution as $ _0 p^*( _0)$. Under this parameterization, the generator receives as input either: (i) samples $x_{ } p_{ 0}(x_{ } _0)$ in the SEDD and MDLM settings, or (ii) soft embeddings $x_{ }^{ }(w_{ }( _0, ))$ in the Duo setting, where $ (0, I)$ and $ _0 p^*( _0)$ is drawn from the data distribution.\n% This design enables the generator to leverage supervision from ground-truth data samples, which can lead to improved generation quality. \n% As a result, the generator is capable of performing multistep inference in the same manner as the teacher model, by numerically integrating the reverse-time dynamics~ .\n\n% % \n% % Specifically, the multistep variant uses the same distillation objective\n% % \n% % \\!\\!\\! _{p_{ }(x_0)} \n% % \\!-\\! _{ }\\; _{p_{ }(x_0)} , \n% % \n% % but parameterizes the student marginal $p_ (x_0)$ as a mixture over intermediate times:\n% % \n% % % p_{ }(x_0)\n% % % = p_{ }(x_0 x_{ t}, t)\\; p^*_{ t}(x_{ t})\\; p( t)\\; dx_{ t}\\, d t . \n% % p_{ }(x_0)\n% % = _{p^*_{ t}(x_{ t}, t)} . \n% % \n% % Here $p^*_{ t}(x_{ t})$ denotes the ground-truth marginal at time $ t$ obtained by running the forward noising process on real data,\n% % and the conditional $p_{ }(x_0 x_{ t}, t)$ is parameterized by the generator $G_ $.\n% % Importantly,~ provides supervision at arbitrary noise levels $ t$, since $(x_{ t}, t)$ can be sampled from the teacher forward process starting from $ x_0 p^*(x_0)$.\n\n% % At inference time, there is no access to samples from $p^*_{ t}(x_{ t})$. The standard way to address it is to substitute them with samples from the model's own intermediate marginals, yielding the self-consistent recursion\n% % \n% % p_{ }(x_0)\n% % _{ p^{ }_{ t}(x_{ t})} \\;\n% % \n% % \\\\\n% % p^{ }_{t'}(x_{t'}) = _{p_{ }(x_0)} , t' < .\n% % \n% % \n% % initialized at the limiting (noise) distribution ${p^{ }_{1}(x_1)=p^{*}_{1}(x_1)}$. This induces a few-step sampling procedure that progressively denoises samples by repeatedly applying the learned conditional model.\n% % \n\n% { {Sequence-Level loss.}}\n% {We now extend the proposed objective to the sequence level. Guided by the formulation in equation~ , we retain the same structural decomposition of the loss. More formally, considering the multistep training procedure, for a fixed sample $x_{ }^{1:L} p_{ 0}(x_{ }^{1:L} _0^{1:L})$ with $ _0^{1:L} p^*( _0^{1:L})$, the sequence-level IDLM objective, corresponding to equation~ , is defined as $ _{ }^{1:L}( ) =$\n% \n% ^{1:L}(f^{*}, G_{ }^{1:L} (x_{ }^{1:L}, ) ) \\!-\\! ^{1:L} ( , G_{ }^{1:L}(x_{ }^{1:L}, ) ). \n% \n% }\n\n% \n% \n% % {Finally}, Algorithm~ outlines the proposed procedure for distilling a pretrained DLM into a few-step generator using the IDLM objective. A visual illustration of the method is provided in Figure~ .\n\n This section specifies what latent space we use in practice and clarifies the remaining implementation details. The training update is summarized in Algorithm~ , the few-step sampler in Algorithm~ , and the full pipeline in Figure~ .\n\n We initialize both the student generator $G_ $ and the auxiliary fake model $f$ from the pretrained teacher $f^*$. Since the teacher has already learned the semantic structure of the token space, this provides a strong initialization and lets the student refine the teacher's predictions rather than learn them from scratch.\n\n We found that directly distilling the model into a one-step generator is difficult in practice, as the student must map terminal noise to a clean sample in a single pass. We therefore allow $G_ $ to produce samples in the few-step regime. Concretely, we set the generator latent input to $ =(x_t,t)$. During training, these inputs are obtained by corrupting real data:\n \\[\n x_0 p^*(x_0), \n t , \n x_t q_t( x_0),\n \\]\n and then feeding $(x_t,t)$ to the student. This multistep parameterization allows the generator to accept intermediate noised states as input, so at inference we can use the same few-step reverse sampling procedure as the teacher model, see Algorithm~ .\n\n All practical updates use the sequence-level loss introduced in the ( ). That is, the token-level objective $ (f,p)$ is replaced by $ ^{1:L}(f,p)$. The forward corruption factorizes across positions, but the teacher, fake model, and student are evaluated on the full noised sequence.",
  "arxiv/sec/experiments.tex": "% !TEX root = ../main.tex\n\n {This section demonstrates the applicability of our IDLM distillation method across several types of DLMs. We conduct the main experiments for MDLM and Duo. Although we focus on these two models, our theoretical formulation also covers the general SEDD setting. Therefore, we report an additional IDLM-SEDD evaluation in Appendix~ . Representative generations are shown in Figure~ . Further implementation details, additional experimental results, and uncurated text samples are provided in Appendices~ ,~ , and~ , respectively.}\n\nWe use the suffix to indicate the teacher: IDLM-MDLM distills MDLM, IDLM-Duo distills the original Duo model, and IDLM-DCD distills the Duo-DCD model. We describe the calculation of all evaluation metrics used in this section in Appendix~ .\n\n Figure~ summarizes how well IDLM distills teachers. For masked diffusion MDLM, IDLM-MDLM reduces the $1024$-step teacher to $16$ reverse steps, giving a ($64 $) acceleration while preserving GenPPL and Entropy metrics. For Duo with the greedy sampler ( ), IDLM-Duo also reaches $16$ steps ($64 $), while IDLM-DCD$^{ }$ reaches $4$ steps ($256 $). The corresponding ancestral Duo results are reported in Appendix~ , where IDLM-DCD$^{ }$ is evaluated down to $4$ steps and gives its best speed-quality tradeoff at $8$ steps ($128 $). Also, GenPPL and Entropy vs sampling-step curves are provided in Figure~ .\n\n The lower panels of Figure~ compare IDLM against other baselines with GenPPL, MAUVE, Entropy, and GM where available. For absorbing-state distillation, IDLM-MDLM gives the best overall low-step tradeoff at $16$ steps, improving GenPPL while preserving diversity. For uniform-state distillation, IDLM-DCD$^{ }$ is strongest in the $8$--$4$ step regime. The ancestral step curve in Figure~ and the detailed results in Table~ show the same trend.\n\n Recent evaluations of flow and diffusion language models show that GenPPL should be interpreted together with sample entropy: lowering entropy can substantially improve GenPPL without necessarily improving the underlying generative distribution. Figure~ visualizes this tradeoff on OWT. IDLM-MDLM improves GenPPL at comparable entropy to the MDLM teacher, and IDLM-DCD remains competitive.\n\n We additionally test IDLM on an $0.9$B MDLM checkpoint from SDTT~ , the full scaling comparison with SDTT is reported in Table~ . The same pattern persists at larger scale: IDLM improves the low-step GenPPL/Entropy tradeoff over SDTT.\n\n We ablate the main IDLM design choices in Appendix~ . The results indicate that the original masked-diffusion update gives the best early GenPPL--Entropy tradeoff, while the full IDLM-Duo update is more stable than its stop-gradient variant. Adding simple Gaussian input noise to IDLM-MDLM does not improve performance (Table~ ).\n\nGenPPL and entropy measure unconditional sample quality, but they do not directly test whether a model preserves sequence-level correctness. We therefore follow the TinyGSM/GSM8K protocol used by~ . The results in Table~ show that IDLM improves steadily with more reverse steps and recovers teacher-level conditional generation with far fewer sampling steps. IDLM-MDLM reaches teacher-level accuracy $19.86\\%$ with only $128$ steps, matching and slightly surpassing the $1024$-step MDLM teacher at $18.0\\%$, corresponding to an ($8 $) reduction in sampling steps. For Duo, IDLM-Duo already reaches comparable accuracy $19.03\\%$ at $64$ steps relative to the $1024$-step Duo teacher at $17.2\\%$, giving a ($16 $) reduction. Overall, IDLM retains conditional generation with far fewer steps.\n\n { }\n\n { Baseline accuracies are reproduced from~ .}\n\n {5pt}\n {0.94}\n {lcc}\n\nModel & Steps & Accuracy (\\%) \\\\\n\n {l}{ } \\\\\nSample & {*}{512} & 53.9 \\\\\nGreedy & & \\\\\n\n {l}{ } \\\\\nMDLM (Teacher)~ & {*}{1024} & \\\\\nDuo (Teacher)~ & & 17.2 \\\\\nFLM~ & & 0.3 \\\\\nS-FLM~ & & \\\\\n\n {l}{ } \\\\\nIDLM-MDLM ( ) & {*}{128} & 19.86 \\\\\nIDLM-Duo ( ) & & \\\\\n\nIDLM-MDLM ( ) & {*}{64} & 14.94 \\\\\nIDLM-Duo ( ) & & \\\\\n\nIDLM-MDLM ( ) & {*}{32} & 12.81 \\\\\nIDLM-Duo ( ) & & \\\\",
  "arxiv/sec/conclusion.tex": "We extend Inverse Distillation to pretrained Diffusion Language Models (DLMs), deriving the IDLM loss ( ) and practical discrete relaxations for non-smooth token spaces ( ). Experiments ( ) show that IDLM accelerates teacher sampling by $4 $--$64 $ across DLM families while preserving generation quality.\n\n Our experiments use relatively small-scale DLMs; a natural next step is to test whether IDLM's few-step gains persist for larger models such as LLaDA~ and on standardized downstream benchmarks."
}